# AutoClick - 开发计划（功能级迭代）V8.0

| 计划版本 | 制定日期 | 制定人 | 预计总工时 | 实际总工时 |
| :--- | :--- | :--- | :--- | :--- |
| V1.0 | 2026-06-13 | AI Assistant | 约 80-100 小时 | — |
| V2.0 | 2026-06-16 | AI Assistant | — | 约 60-70 小时 |
| V2.1 | 2026-06-23 | AI Assistant | +34 小时（Iteration 15~17） | — |
| V3.0 | 2026-06-26 | AI Assistant | +12 小时（Iteration 18） | +12 小时 |
| V3.1 | 2026-06-26 | AI Assistant | +6 小时（Iteration 19） | — |
| V4.0 | 2026-06-27 | AI Assistant | +20 小时（Iteration 20~21） | +20 小时 |
| V5.0 | 2026-06-28 | AI Assistant | +6 小时（Iteration 22） | — |
| V6.0 | 2026-06-29 | AI Assistant | +4 小时（Iteration 23） | — |
| V7.0 | 2026-06-29 | AI Assistant | +4 小时（Iteration 24） | — |
| V8.0 | 2026-06-30 | AI Assistant | +2 小时（Iteration 25） | — |

---

## 1. 迭代总览（实际完成状态）

采用**单功能单迭代**的开发模式，每个迭代完成一个独立可验证的功能模块。

| 迭代 | 功能模块 | 预计工时 | 实际工时 | 交付物 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| Iteration 1 | 项目初始化与环境搭建 | 4h | 4h | 可运行的空应用 | ✅ 已完成 |
| Iteration 2 | 基础框架与布局 | 6h | 6h | 三栏布局界面 + LandingPage双模式 | ✅ 已完成 |
| Iteration 3 | 脚本管理 - 列表展示 | 6h | 10h | 脚本列表显示（含TypeORM service层） | ✅ 已完成 |
| Iteration 4 | 脚本管理 - 导入/导出 | 8h | 2h | 脚本导入导出功能 | ✅ 已完成 |
| Iteration 5 | 步骤列表 - 展示与选中 | 6h | 6h | 步骤列表显示 | ✅ 已完成 |
| Iteration 6 | 步骤编辑 - 编辑/删除 | 8h | 8h | 步骤增删改功能 + 文件夹管理 | ✅ 已完成 |
| Iteration 7 | 设备连接管理 | 10h | 3h | ADB连接设备 + deviceStore | ✅ 已完成 |
| Iteration 8 | 投屏画面显示 | 12h | 4h | 实时投屏（TinyH264解码+Canvas渲染） | ✅ 已完成 |
| Iteration 9 | 投屏交互操作 | 8h | 2h | 点击/滑动/返回/Home + 坐标映射 | ✅ 已完成 |
| Iteration 10 | 脚本执行引擎 | 12h | 12h | 脚本解析与执行（含adb-executor） | ✅ 已完成 |
| Iteration 11 | 步骤高亮与进度反馈 | 6h | 6h | 执行时步骤高亮 + 进度推送 | ✅ 已完成 |
| Iteration 12 | 单步执行功能 | 4h | 4h | 单独执行某步骤 + 重试机制 | ✅ 已完成 |
| Iteration 13 | 深色主题与样式优化 | 4h | 2h | 深色主题CSS变量 + 基础样式 | ⏳ 部分完成 |
| Iteration 14 | 集成测试与Bug修复 | 6h | 6h | 稳定可用版本 + 打包测试 | ✅ 已完成 |
| Iteration 15 | AI智能操控 - 基础链路 | 16h | — | 自然语言指令→ADB执行核心链路（已通过迭代20-21实现） | ✅ 已完成（见迭代20-21） |
| Iteration 16 | AI智能操控 - 工作流与UI | 10h | — | LangGraph编排 + AI面板UI（已通过迭代20-21实现） | ✅ 已完成（见迭代20-21） |
| Iteration 17 | AI智能操控 - 体验增强 | 8h | — | 历史记录 + 多轮上下文 + 配置管理（已通过迭代20-21实现） | ✅ 已完成（见迭代20-21） |
| Iteration 18 | 点击录制功能 | 12h | 12h | 手动录制点击事件 + 模板列表查看/编辑/删除 | ✅ 已完成 |
| Iteration 19 | 步骤类型扩展与体验优化 | 6h | 6h | 新增回主屏幕/打开App/AI步骤类型 + 拖拽排序 + 文档更新 | ✅ 已完成 |
| Iteration 20 | AI智能操控 - 基础链路与工作流 | 16h | 14h | ai-agent解析模块 + ai-assistant编排模块 + 多步骤sequence + check_text文本检测 | ✅ 已完成 |
| Iteration 21 | AI智能操控 - 多任务与动态工具 | 6h | 6h | AI步骤类型 + 动态工具（已录制点击） + 多任务连续执行 + 配置管理(DeepSeek/智谱) | ✅ 已完成 |
| Iteration 22 | 步骤上下文 Context | 6h | — | 前置条件判断 + Context写入 + 模板语法 + UI编辑 | ❌ 未开始 |
| Iteration 23 | 屏幕文本检测步骤 checkText | 4h | — | 新增 checkText 步骤类型：StepType扩展、StepExecutor执行逻辑、UI编辑界面、converter映射、Context集成 | ❌ 未开始 |
| Iteration 24 | 图像识别点击步骤 visionClick | 4h | — | 新增 visionClick 步骤类型：VLM定位+点击、StepExecutor、UI编辑界面、converter映射、文档更新 | ❌ 未开始 |
| Iteration 25 | 等待步骤 wait | 2h | — | 新增 wait 步骤类型：纯延时等待、StepExecutor执行逻辑、UI编辑界面、converter映射、文档更新 | ❌ 未开始 |
| Iteration 20 | AI智能操控 - 基础链路与工作流 | 16h | 14h | ai-agent解析模块 + ai-assistant编排模块 + 多步骤sequence + check_text文本检测 | ✅ 已完成 |
| Iteration 21 | AI智能操控 - 多任务与动态工具 | 6h | 6h | AI步骤类型 + 动态工具（已录制点击） + 多任务连续执行 + 配置管理(DeeSeek/智谱) | ✅ 已完成 |

### 实际迭代变更说明

| 迭代 | 变更说明 |
| Iteration 18 | 点击录制功能独立于脚本步骤，使用独立的 `recorded_clicks` 表；分页查询、编辑/删除功能 |
| Iteration 19 | 新增 home(回主屏幕) 步骤类型，通过 ADB keyevent KEYCODE_HOME 执行；新增 openApp(打开App) 步骤类型，通过 UI Automator 定位应用图标后点击；实现步骤列表拖拽排序（HTML5 Drag & Drop API + 两阶段更新解决唯一约束冲突） |
| :--- | :--- |
| Iteration 3 | 增加TypeORM+SQLite持久化实现，工时从6h增至10h |
| Iteration 4 | service层已在Iteration 3中实现，导入导出仅需IPC+文件对话框，工时从8h降至2h |
| Iteration 6 | 增加文件夹管理功能 |
| Iteration 7 | 使用adb命令行替代@yume-chan/adb库，复杂度降低，工时从10h降至3h |
| Iteration 8 | 使用IPC+TinyH264方案替代WebSocket，工时从12h降至4h |
| Iteration 9 | 交互操作使用adb shell input命令，工时从8h降至2h |
| Iteration 13 | 深色主题CSS变量已配置，但UI组件间样式一致性待优化 |
| Iteration 23 | 新增 checkText(屏幕文本检测) 步骤类型：StepType扩展、StepExecutor执行逻辑、UI编辑界面、converter映射、文档同步 |
| Iteration 24 | 新增 visionClick(图像识别点击) 步骤类型：VLM定位+点击、StepExecutor执行逻辑、UI编辑界面、converter映射、文档同步 |
| Iteration 25 | 新增 wait(等待) 步骤类型：纯延时等待、StepExecutor执行逻辑、UI编辑界面、converter映射、文档更新 |

---

## 2. 详细迭代计划

### Iteration 1：项目初始化与环境搭建 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 搭建Electron + React + TypeScript开发环境 |
| **预计工时** | 4小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-13) |

**任务清单**：
- [x] 初始化Electron项目
- [x] 配置React + TypeScript
- [x] 配置TailwindCSS + shadcn/ui
- [x] 配置主进程/渲染进程目录结构
- [x] 配置开发热重载（concurrently + Vite HMR）
- [x] 验证Hello World可运行

**验收标准**：
- ✅ `npm run dev` 可启动应用并显示空白窗口
- ✅ 开发者工具可正常打开

---

### Iteration 2：基础框架与布局 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实现三栏布局框架，各区域占位 |
| **预计工时** | 6小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-13) |

**任务清单**：
- [x] 实现主窗口1280x860，最小960x600
- [x] 实现三栏Flex布局（脚本栏240px / 步骤栏340px / 投屏区域自适应）
- [x] 实现顶部工具栏 + 底部状态栏
- [x] 各区域添加组件占位
- [x] 支持窗口拖拽缩放

**验收标准**：
- ✅ 布局与设计文档一致
- ✅ 调整窗口大小时布局正常响应
- ✅ 三栏可清晰区分

---

### Iteration 3：脚本管理 - 列表展示 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 显示脚本列表，支持选中切换 + service 层 |
| **预计工时** | 6小时 → 10小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 设计Script/Step数据模型
- [x] 实现TypeORM实体 + SQLite 持久化
- [x] 实现脚本/步骤的增删改查 service 层
- [x] 实现 IPC 通道 + preload 桥接
- [x] 实现脚本列表组件（ScrollArea + Card）
- [x] 实现脚本选中高亮效果
- [x] 实现 Zustand scriptStore（连接 IPC）
- [x] 文件夹/脚本树形展示

**验收标准**：
- ✅ 脚本列表正常显示
- ✅ 点击脚本项可选中并高亮
- ✅ 选中脚本后步骤栏显示对应占位

---

### Iteration 4：脚本管理 - 导入/导出 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实现脚本导入导出功能 |
| **预计工时** | 8小时 → 2小时 |
| **优先级** | P1（重要） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 实现存储模块（SQLite初始化）— 由 Iteration 3 service 层覆盖
- [x] 实现导入功能（文件对话框 → 读取 .js 文件 → 解析 JSON → 入库）
- [x] 实现导出功能（选中脚本 → 保存对话框 → 写入 .js 文件）
- [x] 列表自动刷新（导入后自动 loadScripts）
- [x] 导入格式校验（JSON 格式校验 + 文件类型过滤）
- [x] 导入文件格式：`// comment\n[{type, data}, ...]`

**实现说明**：
- 导入：`script:import` IPC → `dialog.showOpenDialog` → 读取文件 → 解析 JSON → `createScript` + `addStep` 循环入库
- 导出：`script:export` IPC → `getScriptById` + `getStepsByScriptId` → `dialog.showSaveDialog` → 写入格式化的 `.js` 文件
- 导出按钮在未选中脚本时 disabled
- 支持的导入格式：首行为 `//` 注释的 JSON 数组文件

**验收标准**：
- ✅ 可从本地导入`.js`脚本文件
- ✅ 导入后脚本列表自动更新
- ✅ 可导出选中脚本到指定位置
- ✅ 文件格式校验 + 错误提示
- ✅ 重复导入时提示或覆盖

---

### Iteration 5：步骤列表 - 展示与选中 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 显示选中脚本的步骤列表，支持选中 |
| **预计工时** | 6小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 设计Step数据模型
- [x] 实现步骤列表组件（按钮列表 + 图标）
- [x] 实现步骤显示格式化（类型、参数描述）
- [x] 实现步骤选中高亮
- [x] 实现点击步骤时显示操作行（编辑/删除/运行）
- [x] 从数据库读取步骤数据

**验收标准**：
- ✅ 选中脚本后自动显示步骤列表
- ✅ 步骤按序号顺序排列
- ✅ 点击步骤可选中，操作行显示在下方

---

### Iteration 6：步骤编辑 - 编辑/删除 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 支持步骤的增删改操作 |
| **预计工时** | 8小时 |
| **优先级** | P1（重要） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 实现编辑弹窗（Dialog + Form，支持添加/编辑双模式）
- [x] 实现各类型步骤的参数表单（click/type/swipe/longpress/script/home/openApp）
- [x] 实现步骤删除（ConfirmDialog确认）
- [x] 实现步骤增删改后的持久化（数据库同步）
- [x] 实现步骤序号自动重排
- [x] 新增脚本/目录 Tab 切换

**验收标准**：
- ✅ 可编辑步骤参数并保存
- ✅ 可删除步骤，列表实时更新
- ✅ 编辑后脚本文件同步更新

---

### Iteration 7：设备连接管理 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实现Android设备ADB连接 |
| **预计工时** | 10小时 → 3小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 集成 @yume-chan/adb + @yume-chan/adb-server-node-tcp
- [x] 实现设备扫描功能（AdbServerClient.getDevices）
- [x] 实现设备连接与断开（createAdb + close）
- [x] 获取设备信息（serial、model、adb shell getprop）
- [x] 实现 deviceStore 状态管理（status/errorMsg 状态机）
- [x] 底部状态栏显示连接状态
- [x] 连接失败时错误提示（errorMsg 展示）

**验收标准**：
- ✅ 点击连接按钮可扫描到USB设备
- ✅ 连接成功后状态栏显示CONNECTED和设备名
- ✅ 断开连接时状态更新

---

### Iteration 8：投屏画面显示 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实时显示手机屏幕画面 |
| **预计工时** | 12小时 → 4小时 |
| **优先级** | P1（重要） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 确定投屏方案（@yume-chan/scrcpy + TinyH264 解码）
- [x] 实现主进程 scrcpy 会话管理（AdbScrcpyClient）
- [x] 实现视频流读取 → IPC 帧事件推送
- [x] Canvas 渲染（MirrorPanel 画布组件）
- [x] 处理连接/断开时的画面切换
- [x] 画布自适应缩放

**实现说明**：
- 主进程启动 ScrcpyServer，通过 AdbScrcpyClient.videoStream 读取 H264 帧
- 视频帧通过 IPC（scrcpy:frame）发送到渲染进程
- 渲染进程 MirrorPanel 使用 Canvas 显示
- scrcpy-server.jar 通过 fetch-scrcpy-server 自动获取后推送到设备

**验收标准**：
- ✅ 连接设备后 scrcpy 会话启动
- ✅ 视频帧通过 IPC 推送到渲染进程
- ✅ 画布窗口缩放时自适应

---

### Iteration 9：投屏交互操作 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 在投屏画布上模拟触摸操作 |
| **预计工时** | 8小时 → 2小时 |
| **优先级** | P1（重要） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 实现坐标映射函数（Canvas坐标 → 设备坐标）
- [x] 实现单击模拟点击（adb shell input tap）
- [x] 实现返回键（adb shell input keyevent 4）
- [x] 实现 Home 键（adb shell input keyevent 3）
- [x] 画布光标坐标显示

**实现说明**：
- 控制指令通过 adb shell 命令执行（adb.createSocket）
- Canvas 点击 → 坐标缩放转换 → adbShell(`input tap x y`)
- 顶部工具栏添加「返回」和「Home」按钮
- 画布 hover 时实时显示鼠标坐标

**验收标准**：
- ✅ 单击投屏画面，设备有响应
- ✅ 返回键和 Home 键按钮可用
- ✅ 坐标转换准确

---

### Iteration 10：脚本执行引擎 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实现脚本加载与顺序执行 |
| **预计工时** | 12小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 实现ScriptEngine主进程模块（script-engine 目录，含 ScriptEngine/StepExecutor/ExecutionQueue 等）
- [x] 实现脚本文件加载与解析（ScriptLoader 从 ScriptService 加载）
- [x] 实现步骤顺序执行调度（ExecutionQueue FIFO 调度）
- [x] 实现 click/type/swipe/longpress/home/openApp 六种步骤的 ADB 调用（adb-executor + uiautomator-service）
- [x] 实现执行过程中的错误处理与中断（ScriptEngineError + shouldStop）
- [x] 实现 IPC 进度推送（stepStart/stepEnd/stepError/complete 事件）
- [x] device-manager 抽取讨论→决定 script-engine 独立 adb-executor，不依赖 screen-mirror
- [x] 修复坐标映射（CSS坐标→设备坐标，通过 deviceStore 传递 deviceWidth/Height）
- [x] 实现长按步骤（input swipe 同坐标 + 时长）
- [x] 实现中文输入（input text + execFileSync 绕过 shell）

**验收标准**：
- ✅ 点击运行按钮，脚本按顺序执行
- ✅ 点击步骤可正常执行对应操作
- ✅ 执行过程中设备有正确响应

---

### Iteration 11：步骤高亮与进度反馈 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 执行时高亮当前步骤 |
| **预计工时** | 6小时 |
| **优先级** | P2（体验优化） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 渲染进程监听 step-start/step-end/step-error IPC 事件（engine API）
- [x] 实现执行中步骤高亮（黄色背景 + 左边框）
- [x] 执行完成后清除高亮（executingStepIndex = null）
- [x] 底部状态栏显示执行状态（运行中/停止）

**验收标准**：
- ✅ 执行时当前步骤高亮显示
- ✅ 执行完成后高亮清除

---

### Iteration 12：单步执行功能 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 支持单独执行选中的步骤 |
| **预计工时** | 4小时 |
| **优先级** | P2（体验优化） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 实现操作行中的运行按钮（StepForward 图标）
- [x] 实现单个步骤执行逻辑（engine.runSingleStep，仅执行指定 stepIndex）
- [x] 执行时高亮该步骤并清除
- [x] 处理执行错误不影响其他步骤（错误返回 result 而非中断）

**验收标准**：
- ✅ 选中步骤后点击运行，只执行该步骤
- ✅ 执行后高亮正确显示和清除
- ✅ 执行失败时有错误提示（Console 日志）
- ✅ 执行后高亮正确显示和清除
- ✅ 执行失败时有错误提示

---

### Iteration 13：深色主题与样式优化 ⏳

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 完善深色主题视觉一致性 |
| **预计工时** | 4小时 |
| **实际工时** | 2小时 |
| **优先级** | P2（体验优化） |
| **状态** | **部分完成** (2026-06-14) |

**完成情况**：
- [x] 配置shadcn/ui深色主题CSS变量（globals.css中已完成）
- [ ] 按设计规范调整颜色（主色/背景/边框等）— 基础已配置，UI组件间一致性待优化
- [ ] 统一各组件样式 — 基本统一，部分组件hover/active状态待完善
- [ ] 添加hover/active交互状态样式 — 部分组件已实现
- [ ] 调整滚动条样式 — 未实现

**验收标准**：
- ✅ 深色主题CSS变量已配置，应用默认启用深色模式
- ⏳ 部分交互状态有视觉反馈（按钮/列表项hover已实现）
- ❌ 滚动条样式未自定义

---

### Iteration 14：集成测试与Bug修复 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 全功能集成测试，修复关键Bug，打包分发 |
| **预计工时** | 6小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-16) |

**完成情况**：
- [x] 已打包为 macOS dmg 版本（AutoClick-1.0.0.dmg）
- [x] 核心功能链路可用（脚本管理 → 步骤编辑 → 设备连接 → 投屏 → 脚本执行）
- [x] 边界情况测试（无设备/无脚本/大脚本等）
- [x] 异常场景测试（执行中断连接/格式错误等）
- [x] 性能测试（长脚本/连续执行）
- [x] 文档review与更新
- [ ] Windows打包测试

**验收标准**：
- ✅ 已成功打包为macOS桌面应用
- ✅ 所有核心功能可用
- ✅ 文档已review更新
- ❌ Windows打包待测试

---

### Iteration 15~17：AI智能操控 ✅（实际通过迭代20-21实现）

原始计划中的 Iteration 15-17 被重构为更合理的模块划分：

| 原始计划 | 实际实现 | 差异说明 |
| :--- | :--- | :--- |
| Iteration 15：基础链路（DeepSeek Vision+LLM一体） | Iteration 20：ai-agent解析+ai-assistant编排 | 拆分为解析和执行两个子模块，视觉模型改用智谱GLM-4V |
| Iteration 16：工作流与UI | Iteration 20：LangGraph子图+check_text | 移除visual-analysis/coordinate-mapper节点，新增text-check节点 |
| Iteration 17：体验增强 | Iteration 21：动态工具+AI步骤+多任务 | 新增动态工具（已录制点击）、AI步骤类型、多任务连续执行 |

**实际完成情况请参考迭代20-21的具体内容。**
| :--- | :--- |
| **目标** | 实现 LangGraph StateGraph 工作流编排 + AI 操控面板 UI，形成完整可交互的 AI Agent 功能 |
| **预计工时** | 10小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **未开始** |

**涉及的详细设计章节**：5、8、9、12

**实际完成情况请参考迭代20-21的具体内容。**

---

### Iteration 18：点击录制功能 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实现"点击录制"功能：手动在投屏区域点击目标位置，记录点击坐标并保存到数据库；提供录制模板列表查看、编辑、删除功能 |
| **预计工时** | 12小时 |
| **优先级** | P1（重要） |
| **状态** | **已完成** (2026-06-26) |

**涉及的需求章节**：3.8 点击录制（需求文档 V1.4）

**涉及的设计章节**：3.5 点击录制功能、4.5 RecordClickFormDialog、4.6 RecordedClickListDialog（界面文档 V2.2）

**前置依赖**：
- Iteration 7（设备连接管理）— 需获取设备分辨率用于坐标显示
- Iteration 9（投屏交互操作）— 需复用坐标映射逻辑

**任务清单**：

#### 1. 数据层 — Entity（1h）
- [x] 创建 `src/main/script/entities/recorded-click-entity.ts`
  - 定义 `RecordedClickEntity`（TypeORM Entity），表名 `recorded_clicks`
  - 字段：`id`(INTEGER PK AUTOINCREMENT)、`name`(TEXT)、`x`(INTEGER)、`y`(INTEGER)、`type`(TEXT DEFAULT 'click')、`created_at`(DATETIME)、`updated_at`(DATETIME)
- [x] 更新 `src/main/script/data-source.ts`
  - 在 `entities` 数组中注册 `RecordedClickEntity`
  - 在 `CREATE_TABLES_SQL` 中添加 `recorded_clicks` 建表 DDL

#### 2. 数据层 — Repository（1h）
- [x] 创建 `src/main/script/repositories/recorded-click-repository.ts`
  - `create(params)`：创建录制记录
  - `findAll(page, pageSize)`：分页查询，按 `created_at` DESC 排序，返回 `{ items, total, page, pageSize, totalPages }`
  - `findById(id)`：按 ID 查询
  - `update(id, updates)`：更新名称等字段
  - `delete(id)`：删除记录
  - `count()`：查询总记录数

#### 3. 业务层 — Service（1h）
- [x] 创建 `src/main/script/services/recorded-click-service.ts`
  - 遵循 `ScriptService` 的模式，自动 `ensureInitialized()`
  - 封装 CRUD + 分页查询方法

#### 4. IPC 通信（1.5h）
- [x] 更新 `src/main/script/channels.ts`：新增 4 个通道常量
- [x] 更新 `src/main/script/ipc/handlers.ts`：注册 4 个 `ipcMain.handle` 处理器
- [x] 更新 `src/preload/index.ts`：通过 `contextBridge` 暴露 `recordedClick` API

#### 5. 前端组件 — RecordClickFormDialog（2h）
- [x] 创建 `src/renderer/src/components/RecordClickFormDialog.tsx`
  - Dialog，标题："录制点击事件"
  - 字段：名称(Input 自动聚焦 必填)、坐标X(disabled)、坐标Y(disabled)、类型(disabled="click")
  - 底部：取消（保持录制模式）/ 保存（校验→写入DB→关闭弹窗→退出录制模式）
  - 保存中禁用按钮 + 旋转动画，保存失败显示错误提示
  - Props: `open`, `onOpenChange`, `coord: { x, y }`, `onSaved`

#### 6. 前端组件 — RecordedClickListDialog（3h）
- [x] 创建 `src/renderer/src/components/RecordedClickListDialog.tsx`
  - Dialog，标题："录制的点击事件"，宽度 ~520px
  - 列表区域（最小高度 300px）：
    - **加载中**：居中旋转动画
    - **加载失败**：错误提示 + 重试按钮
    - **数据为空**："暂无录制的点击事件" + 📭 图标
    - **有数据**：每行显示名称、坐标 `(x, y)`、类型 badge、创建时间；右侧编辑/删除按钮
  - 编辑模式：行内名称变为 Input + 保存/取消按钮
  - 删除：ConfirmDialog 确认后删除
  - 分页：底部显示"共 N 条，第 M/P 页" + 上一页/页码/下一页

#### 7. MirrorPanel 集成（2.5h）
- [x] 修改 `src/renderer/src/components/MirrorPanel.tsx`
  - 顶部工具栏右侧新增竖向排列的按钮组
  - **录制按钮**：`CircleDot` 图标，激活态红色脉冲，切换 `isRecording` 状态
  - **查看按钮**：`List` 图标，点击打开列表弹窗
  - **蒙版**：录制模式下投屏区域覆盖 `z-30 bg-black/40 cursor-crosshair` 蒙版
  - 点击蒙版获取坐标（需复用 ScreenCanvas 的坐标映射逻辑 `toDevice`）
  - 按 Escape 退出录制模式

#### 8. 步骤编辑弹窗集成（0.5h）
- [x] `NewStepDialog` 点击步骤增加"从录制记录中选择"按钮
- [x] 打开 `RecordedClickListDialog` 选中记录后自动填充坐标

**验收标准**：
- 点击录制按钮 → 投屏区域出现半透明蒙版，鼠标变为十字准星
- 蒙版上点击 → 弹出表单弹窗，坐标自动填入
- 填写名称点击保存 → 数据写入 `recorded_clicks` 表 → 弹窗关闭 → 退出录制模式
- 点击查看按钮 → 弹出录制模板列表，分页显示
- 列表中可编辑名称、可删除记录
- 点击 Escape 退出录制模式
- 录制按钮激活时有红色脉冲动画反馈

---

### Iteration 19：步骤类型扩展与体验优化 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 新增回主屏幕(home)和打开App(openApp)步骤类型，实现步骤列表拖拽排序，更新设计文档 |
| **预计工时** | 6小时 |
| **优先级** | P1（重要） |
| **状态** | **已完成** (2026-06-26) |

**涉及的需求章节**：3.3 脚本引擎（F-07 步骤执行）

**涉及的设计章节**：4.3 步骤类型数据格式（脚本管理详细设计）

**任务清单**：

#### 1. 回主屏幕(home)步骤类型（1.5h）
- [x] 后端：`StepType` 添加 `'home'`，定义 `HomeStepData`（无参数）
- [x] 引擎：`StepExecutor` 添加 `executeHome()` → `adbExec.keyEvent('KEYCODE_HOME')`
- [x] 校验：`ScriptValidator` 添加 `'home'` 到合法类型列表
- [x] 转换：`converter.ts` 添加 `'home'` 的序列化/反序列化
- [x] 前端：`NewStepDialog` 添加"回主屏幕"选择按钮（House图标），`StepPanel` 添加标签与图标

#### 2. 打开App(openApp)步骤类型（2.5h）
- [x] 后端：`StepType` 添加 `'openApp'`，定义 `OpenAppStepData`（含 `appName`）
- [x] 引擎：`StepExecutor` 添加 `executeOpenApp()` → `KEYCODE_HOME` → UI Automator 查找 → 点击坐标
- [x] 服务：复制 `uiautomator-service.ts` 到 `script-engine` 目录，改为独立副本；改用 `adb shell cat` 直接读取 XML 到内存
- [x] 校验/转换：添加 `'openApp'` 支持
- [x] 前端：添加"打开App"选择按钮（AppWindow图标），应用名称输入框 + 提示信息

#### 3. 步骤拖拽排序（1.5h）
- [x] 前端：使用 HTML5 Drag & Drop API，添加 `GripVertical` 拖拽手柄
- [x] 后端修复：`step-repository.ts` `updateOrder()` 改为两阶段更新（先设负值再设目标值），解决 `UNIQUE(script_id, step_index)` 约束冲突
- [x] Store：添加 `setScriptSteps()` 方法，`runScript`/`runStep` 执行前刷新步骤
- [x] 交互：悬停时显示拖拽手柄，拖拽中显示蓝色指示线和半透明效果

#### 4. 文档更新（0.5h）
- [x] 更新 `docs/需求.md`：F-07 更新为7种步骤类型，版本表 V1.5
- [x] 更新 `docs/详细设计/脚本管理详细设计.md`：步骤类型表和实体字段说明
- [x] 更新 `docs/开发计划.md`：添加 Iteration 19

**验收标准**：
- ✅ 添加步骤弹窗可选择"回主屏幕"和"打开App"类型
- ✅ 回主屏幕执行后设备回到桌面
- ✅ 打开App通过 UI Automator 定位应用图标并点击启动
- ✅ 步骤列表可拖拽排序，松开后本地立即更新并持久化到数据库
- ✅ 执行全量/单步脚本时使用拖拽后的最新顺序

---

## 3. 已完成功能总览

| 类别 | 功能 | 状态 |
| :--- | :--- | :--- |
| 脚本管理 | 脚本/文件夹创建、编辑、删除 | ✅ |
| 脚本管理 | 脚本导入/导出（.js文件） | ✅ |
| 脚本管理 | 文件夹树形展开/收起 | ✅ |
| 步骤编辑 | 7种步骤类型：点击/输入/滑动/长按/脚本/回主屏幕/打开App | ✅ |
| 步骤编辑 | 新增、编辑、删除步骤 | ✅ |
| 步骤编辑 | 步骤名称自定义、序号自动重排 | ✅ |
| 步骤编辑 | 步骤列表拖拽排序 | ✅ |
| 步骤编辑 | 从录制记录选择坐标 | ✅ |
| 设备连接 | USB设备自动发现 | ✅ |
| 设备连接 | 设备信息获取（型号/分辨率/传输方式） | ✅ |
| 设备连接 | 连接/断开/连接中状态管理 | ✅ |
| 投屏显示 | scrcpy视频流实时解码渲染 | ✅ |
| 投屏显示 | 画布等比缩放自适应 | ✅ |
| 投屏交互 | 单击/滑动/返回/Home | ✅ |
| 投屏交互 | 实时鼠标坐标显示 | ✅ |
| 投屏交互 | 窗口最小化至投屏尺寸 | ✅ |
| 点击录制 | 点击事件录制（蒙版+表单） | ✅ |
| 点击录制 | 录制模板列表/编辑/删除/分页 | ✅ |
| 脚本执行 | 全量执行/单步执行 | ✅ |
| 脚本执行 | 执行进度推送+步骤高亮 | ✅ |
| 脚本执行 | 超时控制/重试机制/中断执行 | ✅ |
| 脚本执行 | ScriptEngine.runSteps() 动态步骤执行 | ❌ |
| 脚本执行 | home(回主屏幕) + openApp(打开App) 步骤类型 | ✅ |
| 系统配置 | 超时时间/步骤间隔/重试次数配置 | ✅ |
| 深色主题 | 深色主题CSS变量、默认深色模式 | ⏳ |
| 跨平台 | macOS打包（dmg） | ✅ |
| 跨平台 | Windows打包 | ❌ |
| AI智能操控 | DeepSeek API集成（Chat + Vision） | ❌ |
| AI智能操控 | LangGraph工作流编排 | ❌ |
| AI智能操控 | 语义指令→ADB控制命令转换 | ❌ |
| AI智能操控 | VLM屏幕视觉分析（元素识别+坐标定位） | ❌ |
| AI智能操控 | AI指令执行面板（AiAgentPanel） | ❌ |
| AI智能操控 | 多轮对话上下文支持 | ❌ |
| AI智能操控 | 历史记录持久化 | ❌ |

## 4. 未实现/待优化项

| 项目 | 说明 | 优先级 |
| :--- | :--- | :--- |
| AI智能操控 - 基础链路 | DeepSeek集成 + 节点实现 + ScriptEngine.runSteps() | P0 |
| AI智能操控 - 工作流与UI | LangGraph图编排 + AI面板UI | P0 |
| AI智能操控 - 体验增强 | 历史记录持久化 + 多轮上下文 + 配置UI | P1 |
| 中文输入 | adb shell input text 不支持中文 | P2 |
| 中文输入 | adb shell input text 不支持中文 | P2 |
| 自动录制 | 自动录制操作生成脚本 | P3 |
| 通知/Webhook | 飞书/钉钉/微信机器人通知 | P3 |
| 定时任务 | Cron表达式定时执行脚本 | P3 |
| 多设备支持 | 同时连接多台设备 | P4 |
| 滚动条样式 | 自定义滚动条样式 | P4 |
Iteration 3 (脚本列表) Iteration 7 (设备连接)
↓ ↓
├──────────────────┐ Iteration 8 (投屏画面)
↓ ↓ ↓
Iteration 4 (导入导出) Iteration 5 (步骤列表) Iteration 9 (投屏交互)
↓
Iteration 6 (步骤编辑)
↓
Iteration 10 (脚本执行引擎) ──────────────────────┐
↓ ↓
Iteration 11 (步骤高亮) Iteration 20 (AI基础链路与工作流)

---

### Iteration 20：AI智能操控 - 基础链路与工作流 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实现AI智能操控的基础链路：ai-agent解析模块 + ai-assistant编排模块，跑通多步骤sequence和check_text文本检测 |
| **预计工时** | 16小时 |
| **实际工时** | 14小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-27) |

**涉及模块**：
- `src/main/ai-agent/` — 解析子图（LangGraph意图解析→步骤转换）
- `src/main/ai-assistant/` — 编排模块（协调解析+执行+IPC）
- `src/main/common/types.ts` — 公共类型定义

**任务清单**：
- [x] **模块拆分设计**
  - ai-agent（纯解析）：graph.ts定义createParseGraph子图，只做意图解析→步骤转换
  - ai-assistant（编排）：execute-ai-workflow.ts调用parse graph+StepExecutor遍历执行
  - 公共类型合并到common/types.ts，消除循环依赖
- [x] **LangGraph子图（graph.ts）**
  - 使用Annotation.Root定义ParseState
  - 4个节点：intent_parser → screenshot → text_check → step_converter
  - 条件路由：含check_text的走视觉链路，否则直接到step_converter
- [x] **意图理解节点（intent-parser.ts）**
  - 调用DeepSeek Chat解析用户指令
  - 支持action类型：tap/swipe/longPress/input/keyEvent/check_text/sequence
  - 编写intent-parser.md提示词模板
- [x] **截屏节点（screenshot.ts）**
  - 通过adb exec-out screencap -p截取屏幕
  - sharp缩放/编码为Base64
- [x] **屏幕文本检测节点（text-check.ts）**
  - 调用智谱GLM-4V多模态视觉模型
  - 检测屏幕上指定文本是否存在
  - 返回ScreenCheckResult（matched + description）
- [x] **步骤转换节点（step-converter.ts）**
  - 将IntentResult映射为EngineStep[]
  - 支持单步和多步骤sequence
  - check_text结果单独返回，不生成EngineStep
- [x] **AI编排模块（ai-assistant）**
  - execute-ai-workflow.ts：调用parse graph + 遍历执行StepExecutor
  - index.ts：注册IPC处理器（ai-agent:submit/cancel/get-history）
  - 工作流状态推送（status/result/error事件）
  - 历史记录管理（内存存储，最多100条）

**验收标准**：
- ✅ 输入自然语言指令 → ai-agent解析为EngineStep[]
- ✅ 支持单步指令（点击/滑动/回主屏幕）
- ✅ 支持多步骤sequence（复合指令自动拆分）
- ✅ 支持check_text文本检测（调用智谱GLM-4V）
- ✅ ai-assistant遍历执行步骤，返回完整结果
- ✅ IPC推送工作流状态和结果

---

### Iteration 21：AI智能操控 - 多任务与动态工具 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 增强AI智能操控能力：AI步骤类型、动态工具（已录制点击）、多任务连续执行、配置管理 |
| **预计工时** | 6小时 |
| **实际工时** | 6小时 |
| **优先级** | P1（重要） |
| **状态** | **已完成** (2026-06-27) |

**任务清单**：
- [x] **AI步骤类型**
  - common/types.ts中添加'ai'到StepType
  - converter.ts中支持ai类型的序列化/反序列化
  - StepExecutor.executeAi()：调用createParseGraph解析description为子步骤，递归执行
  - UI：StepPanel显示Sparkles图标，NewStepDialog支持AI类型
- [x] **动态工具（已录制点击）**
  - tools/dynamic-tools.ts：从recorded_clicks表加载已录制点击模板
  - 注入intent-parser的提示词，AI可引用已有坐标
  - 减少VLM调用，提高准确率
- [x] **多任务连续执行**
  - execute-ai-workflow.ts：支持多步骤连续执行
  - 步骤间间隔可配置（stepInterval）
  - 失败重试机制（maxRetries）
- [x] **配置管理**
  - 配置拆分：DeepSeek（API Key/Model/BaseURL） + 智谱（API Key/Model/BaseURL）
  - SettingsDialog：独立Tab分别配置DeepSeek和智谱
  - config.ts中定义AiAgentConfig
- [x] **屏幕上下文优化**
  - screen-context.ts：UI Automator扫描屏幕元素
  - uiautomator-service.ts：解析XML布局查找元素
  - 用于openApp时的页面扫描

**验收标准**：
- ✅ AI步骤类型可在脚本中嵌入自然语言描述并自动执行
- ✅ 动态工具从已录制点击中加载，AI可引用
- ✅ 多步骤连续执行成功（如"打开微信，进入聊天，输入Hello，发送"）
- ✅ DeepSeek和智谱分别独立配置
- ✅ UI显示AI助手面板和工作流状态
↓ ↓
Iteration 12 (单步执行) Iteration 20 (AI基础链路与工作流)
↓ ↓
Iteration 13 (深色主题) Iteration 17 (AI体验增强)
↓
Iteration 14 (集成测试)


---

## 5. 风险与应对

| 风险项 | 概率 | 影响 | 应对措施 |
| :--- | :--- | :--- | :--- |
| ADB兼容性问题 | 中 | 高 | 提前验证多设备/多Android版本，准备降级方案 |
| 投屏延迟过高 | 中 | 中 | 调研多种方案（minicap/scrcpy协议），选用最优 |
| 脚本沙箱安全漏洞 | 低 | 高 | 使用成熟的vm2，严格限制权限 |
| Electron打包跨平台问题 | 中 | 中 | 提前测试Windows/macOS环境，配置CI |
| 开发时间超出预期 | 中 | 中 | 优先保证P0功能，P2功能可延后 |

---

## 6. 开发顺序建议

**建议按以下顺序执行：**

1. **第一阶段（基础底座）**：Iteration 1 → 2
2. **第二阶段（脚本核心）**：Iteration 3 → 5 → 6
3. **第三阶段（设备投屏）**：Iteration 7 → 8 → 9（可与第二阶段并行）
4. **第四阶段（执行引擎）**：Iteration 10 → 11 → 12
5. **第五阶段（完善发布）**：Iteration 4 → 13 → 14

**并行开发建议**：
- 完成Iteration 2后，可分配2人分别进行 Iteration 3（脚本）和 Iteration 7（设备）
- 完成Iteration 5后，可分配2人分别进行 Iteration 6（步骤编辑）和 Iteration 8（投屏）

---

### Iteration 22：步骤上下文 Context ❌

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 为脚本执行引擎添加 Context 机制：前置条件判断 + Context写入 + 模板语法 |
| **预计工时** | 6小时 |
| **优先级** | P1（重要） |
| **状态** | **未开始** |

**涉及的需求章节**：3.9 步骤上下文 Context

**涉及的设计章节**：7.4~7.6 Context 数据结构、模板解析、执行流程

**任务清单**：

#### 1. 数据模型与类型（1h）
- [ ] common/types.ts 新增 `StepCondition`、`StepContextOutput` 接口
- [ ] ExecutionContext 新增 `context: Record<string, string>` 字段
- [ ] execution-context.ts 初始化 context 为空对象 `{}`

#### 2. 模板解析函数（0.5h）
- [ ] 实现 `resolveTemplates(data, context)` 函数
- [ ] 递归扫描 data 中所有字符串值，将 `{{key}}` 替换为 `context[key]`
- [ ] 注意：数字 `"320"` → 字符串，与现有 Number(x) 转换兼容

#### 3. StepExecutor 改造（1.5h）
- [ ] execute() 中增加前置条件判断逻辑：
  - 解析 _condition.key 和 _condition.value（支持模板）
  - 比较 `context[key] === value`
  - 匹配则按 onMatch 跳过或停止
- [ ] 步骤执行完成后检查 _context，写入 context
- [ ] 模板解析：执行前解析 data 中所有字符串

#### 4. 脚本引擎集成（1h）
- [ ] script-engine.ts 执行循环中传递 context
- [ ] 脚本加载时初始化 context
- [ ] 全量执行/单步执行都支持 context

#### 5. UI — 步骤列表展示（0.5h）
- [ ] StepPanel 步骤行下方显示条件和 context 信息（小字）
- [ ] 条件图标 `GitBranch`，写入图标 `Variable`

#### 6. UI — 步骤编辑弹窗（1.5h）
- [ ] NewStepDialog 新增两个折叠面板（Collapsible）：
  - "前置条件"：key 输入框 + value 输入框 + onMatch 选择（跳过/停止）
  - "写入 Context"：key 输入框 + value 输入框
- [ ] 编辑模式读取已有 _condition / _context 回填

**验收标准**：
- 步骤可配置前置条件，满足条件时跳过或停止
- 步骤执行后可写入 Context 数据
- 步骤参数支持 `{{key}}` 模板语法
- UI 可查看和编辑这些配置
- 模板解析与现有 Number(x) 兼容

---

**文档状态**：待确认

---

### Iteration 25：等待步骤 wait

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 新增 `wait` 步骤类型，支持脚本中插入指定时长的延时等待 |
| **预计工时** | 2小时 |
| **优先级** | P1（重要） |
| **状态** | ❌ 未开始 |

**涉及的需求章节**：3.11 等待步骤

**涉及的设计章节**：详细设计/等待步骤详细设计.md

**任务清单**：

#### 1. 公共类型扩展（0.3h）
- [ ] `src/main/common/types.ts`：`StepType` 追加 `'wait'`
- [ ] 新增 `WaitStepData` 接口（duration: number, description?: string）
- [ ] `StepData` 联合类型追加 `WaitStepData`

#### 2. 执行引擎（0.5h）
- [ ] `src/main/script-engine/step-executor.ts`：`switch` 中新增 `case 'wait'`
- [ ] 实现 `executeWait()` 私有方法：通过 Promise + setTimeout 延时
- [ ] 校验 duration > 0，否则抛错
- [ ] 支持中断取消（监听 context 取消信号）

#### 3. IPC 数据转换（0.2h）
- [ ] `src/main/script/ipc/converter.ts`：`case 'wait'` 映射参数描述 `N.Ns`

#### 4. UI 组件（0.8h）
- [ ] `src/renderer/src/components/StepPanel.tsx`：追加 `wait: '等待'` 标签和 `Hourglass` 图标
- [ ] `src/renderer/src/components/NewStepDialog.tsx`：类型选择追加 wait 条目（标签、图标、颜色 text-amber-500）；表单渲染追加 `case 'wait'` 分支（等待时长输入+原因说明）
- [ ] `src/renderer/src/stores/scriptStore.ts`：`StepType` 类型追加 `'wait'`

#### 5. 文档更新（0.2h）
- [ ] 需求文档、界面文档、开发计划已在本迭代前同步完成

**验收标准**：
- 新建 wait 步骤时输入等待时长，保存后步骤列表显示 ⏳ 图标 + "等待" + "N.Ns"
- 执行脚本时 wait 步骤暂停指定时长后继续
- duration 支持小数（如 0.5 = 500ms）
- duration ≤ 0 时报错
- 执行中手动取消脚本可中断等待
- 导入导出含 wait 步骤的脚本正常


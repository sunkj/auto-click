# AutoClick - 开发计划（功能级迭代）V3.0

| 计划版本 | 制定日期 | 制定人 | 预计总工时 | 实际总工时 |
| :--- | :--- | :--- | :--- | :--- |
| V1.0 | 2026-06-13 | AI Assistant | 约 80-100 小时 | — |
| V2.0 | 2026-06-16 | AI Assistant | — | 约 60-70 小时 |
| V2.1 | 2026-06-23 | AI Assistant | +34 小时（Iteration 15~17） | — |
| V3.0 | 2026-06-26 | AI Assistant | +12 小时（Iteration 18） | +12 小时 |
| V3.1 | 2026-06-26 | AI Assistant | +6 小时（Iteration 19） | — |

---

## 1. 迭代总览（实际完成状态）

采用**单功能单迭代**的开发模式，每个迭代完成一个独立可验证的功能模块。

| 迭代 | 功能模块 | 预计工时 | 实际工时 | 交付物 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| Iteration 1 | 项目初始化与环境搭建 | 4h | 4h | 可运行的空应用 | ✅ 已完成 |
| Iteration 2 | 基础框架与布局 | 6h | 6h | 三栏布局界面 + LandingPage双模式 | ✅ 已完成 |
| Iteration 3 | 脚本管理 - 列表展示 | 6h | 10h | 脚本列表显示（含TypeORM service层） | ✅ 已完成 |
| Iteration 4 | 脚本管理 - 导入/导出 | 8h | 2h | 脚本导入导出功能 | ✅ 已完成 |
| Iteration 5 | 步骤列表 - 展示与选中 | 6h | 6h | 步骤列表显示 | ✅ 已完成 |
| Iteration 6 | 步骤编辑 - 编辑/删除 | 8h | 8h | 步骤增删改功能 + 文件夹管理 | ✅ 已完成 |
| Iteration 7 | 设备连接管理 | 10h | 3h | ADB连接设备 + deviceStore | ✅ 已完成 |
| Iteration 8 | 投屏画面显示 | 12h | 4h | 实时投屏（TinyH264解码+Canvas渲染） | ✅ 已完成 |
| Iteration 9 | 投屏交互操作 | 8h | 2h | 点击/滑动/返回/Home + 坐标映射 | ✅ 已完成 |
| Iteration 10 | 脚本执行引擎 | 12h | 12h | 脚本解析与执行（含adb-executor） | ✅ 已完成 |
| Iteration 11 | 步骤高亮与进度反馈 | 6h | 6h | 执行时步骤高亮 + 进度推送 | ✅ 已完成 |
| Iteration 12 | 单步执行功能 | 4h | 4h | 单独执行某步骤 + 重试机制 | ✅ 已完成 |
| Iteration 13 | 深色主题与样式优化 | 4h | 2h | 深色主题CSS变量 + 基础样式 | ⏳ 部分完成 |
| Iteration 14 | 集成测试与Bug修复 | 6h | 6h | 稳定可用版本 + 打包测试 | ✅ 已完成 |
| Iteration 15 | AI智能操控 - 基础链路 | 16h | — | 自然语言指令→ADB执行核心链路 | ❌ 未开始 |
| Iteration 16 | AI智能操控 - 工作流与UI | 10h | — | LangGraph编排 + AI面板UI | ❌ 未开始 |
| Iteration 17 | AI智能操控 - 体验增强 | 8h | — | 历史记录 + 多轮上下文 + 配置管理 | ❌ 未开始 |
| Iteration 18 | 点击录制功能 | 12h | 12h | 手动录制点击事件 + 模板列表查看/编辑/删除 | ✅ 已完成 |
| Iteration 19 | 步骤类型扩展与体验优化 | 6h | — | 新增回主屏幕/打开App步骤类型 + 拖拽排序 + 文档更新 | ✅ 已完成 |

### 实际迭代变更说明

| 迭代 | 变更说明 |
| Iteration 18 | 点击录制功能独立于脚本步骤，使用独立的 `recorded_clicks` 表；分页查询、编辑/删除功能 |
| Iteration 19 | 新增 home(回主屏幕) 步骤类型，通过 ADB keyevent KEYCODE_HOME 执行；新增 openApp(打开App) 步骤类型，通过 UI Automator 定位应用图标后点击；实现步骤列表拖拽排序（HTML5 Drag & Drop API + 两阶段更新解决唯一约束冲突） |
| :--- | :--- |
| Iteration 3 | 增加TypeORM+SQLite持久化实现，工时从6h增至10h |
| Iteration 4 | service层已在Iteration 3中实现，导入导出仅需IPC+文件对话框，工时从8h降至2h |
| Iteration 6 | 增加文件夹管理功能 |
| Iteration 7 | 使用adb命令行替代@yume-chan/adb库，复杂度降低，工时从10h降至3h |
| Iteration 8 | 使用IPC+TinyH264方案替代WebSocket，工时从12h降至4h |
| Iteration 9 | 交互操作使用adb shell input命令，工时从8h降至2h |
| Iteration 13 | 深色主题CSS变量已配置，但UI组件间样式一致性待优化 |

---

## 2. 详细迭代计划

### Iteration 1：项目初始化与环境搭建 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 搭建Electron + React + TypeScript开发环境 |
| **预计工时** | 4小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-13) |

**任务清单**：
- [x] 初始化Electron项目
- [x] 配置React + TypeScript
- [x] 配置TailwindCSS + shadcn/ui
- [x] 配置主进程/渲染进程目录结构
- [x] 配置开发热重载（concurrently + Vite HMR）
- [x] 验证Hello World可运行

**验收标准**：
- ✅ `npm run dev` 可启动应用并显示空白窗口
- ✅ 开发者工具可正常打开

---

### Iteration 2：基础框架与布局 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实现三栏布局框架，各区域占位 |
| **预计工时** | 6小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-13) |

**任务清单**：
- [x] 实现主窗口1280x860，最小960x600
- [x] 实现三栏Flex布局（脚本栏240px / 步骤栏340px / 投屏区域自适应）
- [x] 实现顶部工具栏 + 底部状态栏
- [x] 各区域添加组件占位
- [x] 支持窗口拖拽缩放

**验收标准**：
- ✅ 布局与设计文档一致
- ✅ 调整窗口大小时布局正常响应
- ✅ 三栏可清晰区分

---

### Iteration 3：脚本管理 - 列表展示 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 显示脚本列表，支持选中切换 + service 层 |
| **预计工时** | 6小时 → 10小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 设计Script/Step数据模型
- [x] 实现TypeORM实体 + SQLite 持久化
- [x] 实现脚本/步骤的增删改查 service 层
- [x] 实现 IPC 通道 + preload 桥接
- [x] 实现脚本列表组件（ScrollArea + Card）
- [x] 实现脚本选中高亮效果
- [x] 实现 Zustand scriptStore（连接 IPC）
- [x] 文件夹/脚本树形展示

**验收标准**：
- ✅ 脚本列表正常显示
- ✅ 点击脚本项可选中并高亮
- ✅ 选中脚本后步骤栏显示对应占位

---

### Iteration 4：脚本管理 - 导入/导出 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实现脚本导入导出功能 |
| **预计工时** | 8小时 → 2小时 |
| **优先级** | P1（重要） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 实现存储模块（SQLite初始化）— 由 Iteration 3 service 层覆盖
- [x] 实现导入功能（文件对话框 → 读取 .js 文件 → 解析 JSON → 入库）
- [x] 实现导出功能（选中脚本 → 保存对话框 → 写入 .js 文件）
- [x] 列表自动刷新（导入后自动 loadScripts）
- [x] 导入格式校验（JSON 格式校验 + 文件类型过滤）
- [x] 导入文件格式：`// comment\n[{type, data}, ...]`

**实现说明**：
- 导入：`script:import` IPC → `dialog.showOpenDialog` → 读取文件 → 解析 JSON → `createScript` + `addStep` 循环入库
- 导出：`script:export` IPC → `getScriptById` + `getStepsByScriptId` → `dialog.showSaveDialog` → 写入格式化的 `.js` 文件
- 导出按钮在未选中脚本时 disabled
- 支持的导入格式：首行为 `//` 注释的 JSON 数组文件

**验收标准**：
- ✅ 可从本地导入`.js`脚本文件
- ✅ 导入后脚本列表自动更新
- ✅ 可导出选中脚本到指定位置
- ✅ 文件格式校验 + 错误提示
- ✅ 重复导入时提示或覆盖

---

### Iteration 5：步骤列表 - 展示与选中 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 显示选中脚本的步骤列表，支持选中 |
| **预计工时** | 6小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 设计Step数据模型
- [x] 实现步骤列表组件（按钮列表 + 图标）
- [x] 实现步骤显示格式化（类型、参数描述）
- [x] 实现步骤选中高亮
- [x] 实现点击步骤时显示操作行（编辑/删除/运行）
- [x] 从数据库读取步骤数据

**验收标准**：
- ✅ 选中脚本后自动显示步骤列表
- ✅ 步骤按序号顺序排列
- ✅ 点击步骤可选中，操作行显示在下方

---

### Iteration 6：步骤编辑 - 编辑/删除 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 支持步骤的增删改操作 |
| **预计工时** | 8小时 |
| **优先级** | P1（重要） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 实现编辑弹窗（Dialog + Form，支持添加/编辑双模式）
- [x] 实现各类型步骤的参数表单（click/type/swipe/longpress/script/home/openApp）
- [x] 实现步骤删除（ConfirmDialog确认）
- [x] 实现步骤增删改后的持久化（数据库同步）
- [x] 实现步骤序号自动重排
- [x] 新增脚本/目录 Tab 切换

**验收标准**：
- ✅ 可编辑步骤参数并保存
- ✅ 可删除步骤，列表实时更新
- ✅ 编辑后脚本文件同步更新

---

### Iteration 7：设备连接管理 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实现Android设备ADB连接 |
| **预计工时** | 10小时 → 3小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 集成 @yume-chan/adb + @yume-chan/adb-server-node-tcp
- [x] 实现设备扫描功能（AdbServerClient.getDevices）
- [x] 实现设备连接与断开（createAdb + close）
- [x] 获取设备信息（serial、model、adb shell getprop）
- [x] 实现 deviceStore 状态管理（status/errorMsg 状态机）
- [x] 底部状态栏显示连接状态
- [x] 连接失败时错误提示（errorMsg 展示）

**验收标准**：
- ✅ 点击连接按钮可扫描到USB设备
- ✅ 连接成功后状态栏显示CONNECTED和设备名
- ✅ 断开连接时状态更新

---

### Iteration 8：投屏画面显示 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实时显示手机屏幕画面 |
| **预计工时** | 12小时 → 4小时 |
| **优先级** | P1（重要） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 确定投屏方案（@yume-chan/scrcpy + TinyH264 解码）
- [x] 实现主进程 scrcpy 会话管理（AdbScrcpyClient）
- [x] 实现视频流读取 → IPC 帧事件推送
- [x] Canvas 渲染（MirrorPanel 画布组件）
- [x] 处理连接/断开时的画面切换
- [x] 画布自适应缩放

**实现说明**：
- 主进程启动 ScrcpyServer，通过 AdbScrcpyClient.videoStream 读取 H264 帧
- 视频帧通过 IPC（scrcpy:frame）发送到渲染进程
- 渲染进程 MirrorPanel 使用 Canvas 显示
- scrcpy-server.jar 通过 fetch-scrcpy-server 自动获取后推送到设备

**验收标准**：
- ✅ 连接设备后 scrcpy 会话启动
- ✅ 视频帧通过 IPC 推送到渲染进程
- ✅ 画布窗口缩放时自适应

---

### Iteration 9：投屏交互操作 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 在投屏画布上模拟触摸操作 |
| **预计工时** | 8小时 → 2小时 |
| **优先级** | P1（重要） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 实现坐标映射函数（Canvas坐标 → 设备坐标）
- [x] 实现单击模拟点击（adb shell input tap）
- [x] 实现返回键（adb shell input keyevent 4）
- [x] 实现 Home 键（adb shell input keyevent 3）
- [x] 画布光标坐标显示

**实现说明**：
- 控制指令通过 adb shell 命令执行（adb.createSocket）
- Canvas 点击 → 坐标缩放转换 → adbShell(`input tap x y`)
- 顶部工具栏添加「返回」和「Home」按钮
- 画布 hover 时实时显示鼠标坐标

**验收标准**：
- ✅ 单击投屏画面，设备有响应
- ✅ 返回键和 Home 键按钮可用
- ✅ 坐标转换准确

---

### Iteration 10：脚本执行引擎 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实现脚本加载与顺序执行 |
| **预计工时** | 12小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 实现ScriptEngine主进程模块（script-engine 目录，含 ScriptEngine/StepExecutor/ExecutionQueue 等）
- [x] 实现脚本文件加载与解析（ScriptLoader 从 ScriptService 加载）
- [x] 实现步骤顺序执行调度（ExecutionQueue FIFO 调度）
- [x] 实现 click/type/swipe/longpress/home/openApp 六种步骤的 ADB 调用（adb-executor + uiautomator-service）
- [x] 实现执行过程中的错误处理与中断（ScriptEngineError + shouldStop）
- [x] 实现 IPC 进度推送（stepStart/stepEnd/stepError/complete 事件）
- [x] device-manager 抽取讨论→决定 script-engine 独立 adb-executor，不依赖 screen-mirror
- [x] 修复坐标映射（CSS坐标→设备坐标，通过 deviceStore 传递 deviceWidth/Height）
- [x] 实现长按步骤（input swipe 同坐标 + 时长）
- [x] 实现中文输入（input text + execFileSync 绕过 shell）

**验收标准**：
- ✅ 点击运行按钮，脚本按顺序执行
- ✅ 点击步骤可正常执行对应操作
- ✅ 执行过程中设备有正确响应

---

### Iteration 11：步骤高亮与进度反馈 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 执行时高亮当前步骤 |
| **预计工时** | 6小时 |
| **优先级** | P2（体验优化） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 渲染进程监听 step-start/step-end/step-error IPC 事件（engine API）
- [x] 实现执行中步骤高亮（黄色背景 + 左边框）
- [x] 执行完成后清除高亮（executingStepIndex = null）
- [x] 底部状态栏显示执行状态（运行中/停止）

**验收标准**：
- ✅ 执行时当前步骤高亮显示
- ✅ 执行完成后高亮清除

---

### Iteration 12：单步执行功能 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 支持单独执行选中的步骤 |
| **预计工时** | 4小时 |
| **优先级** | P2（体验优化） |
| **状态** | **已完成** (2026-06-14) |

**任务清单**：
- [x] 实现操作行中的运行按钮（StepForward 图标）
- [x] 实现单个步骤执行逻辑（engine.runSingleStep，仅执行指定 stepIndex）
- [x] 执行时高亮该步骤并清除
- [x] 处理执行错误不影响其他步骤（错误返回 result 而非中断）

**验收标准**：
- ✅ 选中步骤后点击运行，只执行该步骤
- ✅ 执行后高亮正确显示和清除
- ✅ 执行失败时有错误提示（Console 日志）
- ✅ 执行后高亮正确显示和清除
- ✅ 执行失败时有错误提示

---

### Iteration 13：深色主题与样式优化 ⏳

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 完善深色主题视觉一致性 |
| **预计工时** | 4小时 |
| **实际工时** | 2小时 |
| **优先级** | P2（体验优化） |
| **状态** | **部分完成** (2026-06-14) |

**完成情况**：
- [x] 配置shadcn/ui深色主题CSS变量（globals.css中已完成）
- [ ] 按设计规范调整颜色（主色/背景/边框等）— 基础已配置，UI组件间一致性待优化
- [ ] 统一各组件样式 — 基本统一，部分组件hover/active状态待完善
- [ ] 添加hover/active交互状态样式 — 部分组件已实现
- [ ] 调整滚动条样式 — 未实现

**验收标准**：
- ✅ 深色主题CSS变量已配置，应用默认启用深色模式
- ⏳ 部分交互状态有视觉反馈（按钮/列表项hover已实现）
- ❌ 滚动条样式未自定义

---

### Iteration 14：集成测试与Bug修复 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 全功能集成测试，修复关键Bug，打包分发 |
| **预计工时** | 6小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **已完成** (2026-06-16) |

**完成情况**：
- [x] 已打包为 macOS dmg 版本（AutoClick-1.0.0.dmg）
- [x] 核心功能链路可用（脚本管理 → 步骤编辑 → 设备连接 → 投屏 → 脚本执行）
- [x] 边界情况测试（无设备/无脚本/大脚本等）
- [x] 异常场景测试（执行中断连接/格式错误等）
- [x] 性能测试（长脚本/连续执行）
- [x] 文档review与更新
- [ ] Windows打包测试

**验收标准**：
- ✅ 已成功打包为macOS桌面应用
- ✅ 所有核心功能可用
- ✅ 文档已review更新
- ❌ Windows打包待测试

---

### Iteration 15：AI智能操控 - 基础链路 ❌

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实现 DeepSeek API 集成与核心 AI Agent 节点，跑通"自然语言指令 → ADB 命令执行"的 MVP 链路 |
| **预计工时** | 16小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **未开始** |

**涉及的详细设计章节**：6.1~6.4、7、10、11、12

**前置依赖**：
- Iteration 10（脚本执行引擎）— 需复用 `StepExecutor` 和 `ExecutionQueue`
- Iteration 7（设备连接管理）— 需获取 `deviceSerial` 和 `deviceResolution`

**任务清单**：
- [ ] **DeepSeek 服务封装**（`services/deepseek-service.ts`）
  - 封装 Chat 和 Vision 两类 API 调用
  - 基于 `@langchain/openai`，配置 baseURL 指向 DeepSeek
  - 实现请求重试、超时控制、错误处理
  - 预计工时：2h

- [ ] **截图服务**（`services/screen-service.ts`）
  - 通过 `adb exec-out screencap -p` 截取手机屏幕
  - 使用 `sharp` 缩放图片至最大 1024px 并编码为 Base64
  - 实现截图缓存（最近 3 帧变化检测）
  - 预计工时：1.5h

- [ ] **意图理解节点**（`nodes/intent-parser.ts`）
  - 编写 `prompts/intent-parser.md` 提示词模板
  - 调用 DeepSeek Chat 解析用户指令为结构化 `IntentResult`
  - 支持动作类型：tap/swipe/longPress/input/keyEvent
  - 预计工时：2h

- [ ] **视觉分析节点**（`nodes/visual-analysis.ts`）
  - 编写 `prompts/visual-analysis.md` 提示词模板
  - 构造多模态消息（截图 + 目标描述），调用 DeepSeek Vision
  - 解析 VLM 返回的坐标与元素描述
  - 预计工时：2h

- [ ] **坐标映射节点**（`nodes/coordinate-mapper.ts`）
  - 实现 `utils/coordinate-utils.ts`（相对/绝对坐标转换、缩放比例校准）
  - 将 VLM 返回坐标映射回设备原始分辨率
  - 预计工时：1h

- [ ] **步骤转换节点**（`nodes/step-converter.ts`）
  - 将 AI 解析结果（`IntentResult` + `CalibratedCoord`）映射为 `EngineStep[]`
  - 实现动作类型到 EngineStep 的完整映射表
  - 预计工时：1h

- [ ] **ScriptEngine.runSteps() 方法**（修改 `script-engine/script-engine.ts`）
  - 新增 `runSteps(steps: EngineStep[], serial: string)` 方法
  - 复用现有 `StepExecutor`、`ExecutionQueue`、IPC 进度推送
  - 绕过数据库加载，直接执行传入的步骤列表
  - 返回 `EngineExecutionResult`（含每步结果）
  - 预计工时：2h

- [ ] **引擎执行节点**（`nodes/script-engine-executor.ts`）
  - 调用 `ScriptEngine.runSteps()` ，将转换后的步骤传入并执行
  - 等待执行结果，更新 `AgentState.result`
  - 预计工时：0.5h

- [ ] **类型定义**（`types.ts`）
  - 定义 `AgentState`、`IntentResult`、`VisualResult`、`CalibratedCoord`、`EngineExecutionResult`、`AgentError` 等全部类型
  - 定义 IPC 消息类型 `AiAgentIpcEvent`
  - 预计工时：0.5h

- [ ] **配置系统**（`config.ts`）
  - 定义 `AiAgentConfig` 接口（API Key、模型名、超时、截图参数等）
  - API Key 使用 Electron `safeStorage` 加密存储
  - 预计工时：1h

**验收标准**：
- 可通过 IPC 或测试脚本调用完成"输入指令 → 意图理解 → 截图 → 视觉分析 → 坐标映射 → 步骤转换 → ScriptEngine 执行"的完整链路
- 支持 3 种以上基础指令（点击、滑动、长按、返回桌面）
- 意图理解能正确解析动作类型和目标语义
- VLM 能识别截图中的 UI 元素并返回合理坐标
- 坐标映射能将 VLM 坐标正确转换为设备分辨率坐标
- `ScriptEngine.runSteps()` 能独立执行传入的 `EngineStep[]`，不与现有脚本执行功能冲突

---

### Iteration 16：AI智能操控 - 工作流与UI ❌

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实现 LangGraph StateGraph 工作流编排 + AI 操控面板 UI，形成完整可交互的 AI Agent 功能 |
| **预计工时** | 10小时 |
| **优先级** | P0（阻塞级） |
| **状态** | **未开始** |

**涉及的详细设计章节**：5、8、9、12

**前置依赖**：
- Iteration 15（基础链路）— 所有节点和服务已就绪

**任务清单**：

- [ ] **LangGraph StateGraph 图定义**（`graph.ts`）
  - 使用 `@langchain/langgraph` 的 `StateGraph` 构建工作流
  - 注册 6 个节点：intent_parser → screenshot → visual_analysis → coordinate_mapper → step_converter → script_engine_executor
  - 实现条件路由：keyEvent 跳过视觉链路直达步骤转换
  - 编译为可执行应用（`graph.compile()`）
  - 预计工时：3h

- [ ] **IPC 通道与模块入口**（`index.ts` + `channels.ts`）
  - 定义 `AI_AGENT_CHANNELS` IPC 通道常量
  - 实现 IPC handler：`ai-agent:submit`（启动工作流）、`ai-agent:cancel`（中断）
  - 实现工作流状态推送（`ai-agent:status`）：实时推送当前节点名称和消息
  - 实现结果推送（`ai-agent:result` / `ai-agent:error`）
  - 通过 `callbacks.handleNodeStart` 在每个节点启动时推送状态
  - 预计工时：2h

- [ ] **AI 操控面板 UI**（`AiAgentPanel.tsx`）
  - 实现输入区域：文本输入框 + 发送按钮 + 快捷指令下拉
  - 实现状态展示区：工作流状态指示器 + 当前节点名称 + 进度动画
  - 实现结果展示区：执行成功/失败标记 + 步骤摘要（总步骤数/完成数/耗时）
  - 实现历史记录区：历史指令列表（时间倒序）+ 清空按钮
  - 预计工时：3h

- [ ] **Zustand 状态管理**（`aiAgentStore.ts`）
  - 管理 `workflowStatus`、`currentNode`、`statusMessage`
  - 管理 `lastResult`、`lastError`
  - 管理 `history` 列表
  - 实现 actions：`submit`、`cancel`、`clearHistory`
  - 预计工时：1h

- [ ] **集成测试与调试**
  - 测试完整 UI → IPC → LangGraph → 执行 → 结果反馈链路的端到端流程
  - 调试 UI 状态同步和动画
  - 预计工时：1h

**验收标准**：
- AI Agent 面板在侧栏或独立区域正常显示
- 输入指令后，面板实时显示工作流状态（"正在理解指令..." → "正在截取屏幕..." → "正在分析屏幕内容..." → "正在执行..."）
- 执行完成后正确显示成功/失败结果和步骤摘要
- 可通过面板取消正在执行的工作流
- 历史记录正确保存和展示

---

### Iteration 17：AI智能操控 - 体验增强 ❌

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 完善 AI Agent 的错误处理、配置管理、多轮上下文和历史记录持久化，提升用户体验和健壮性 |
| **预计工时** | 8小时 |
| **优先级** | P1~P2（体验优化） |
| **状态** | **未开始** |

**涉及的详细设计章节**：13、14、15、16

**前置依赖**：
- Iteration 16（工作流与UI）— UI 和工作流已就绪

**任务清单**：

- [ ] **错误处理与重试机制**
  - 实现各节点的超时控制（`nodeTimeout` 配置）
  - 实现 LLM/VLM/截图失败的自动重试（指数退避）
  - 实现 `COORDINATE_INVALID` 错误时向用户提示
  - 错误信息通过 IPC 推送至 UI 展示
  - 预计工时：2h

- [ ] **多轮对话上下文**
  - 在 `AgentState.history` 中维护历史记录列表
  - 每次工作流完成后，将结果追加到 history
  - 意图理解节点注入最近 3 条历史记录作为上下文提示
  - 支持"点这里"、"再往下滑一点"等依赖上下文的模糊指令
  - 预计工时：2h

- [ ] **历史记录持久化**
  - 使用 SQLite 或 JSON 文件持久化历史记录
  - 应用重启后可查看历史指令记录
  - 支持清空历史功能
  - 预计工时：1.5h

- [ ] **配置管理 UI**
  - 在设置面板中增加 AI 配置区域
  - DeepSeek API Key 输入框（密码模式，加密存储）
  - 模型选择/超时时间/截图质量等配置项
  - 预计工时：1.5h

- [ ] **错误提示与边界场景处理**
  - 设备未连接时的友好提示
  - 屏幕锁定/黑屏时的检测与提示
  - API Key 未配置时的配置引导
  - 网络断开时的重连提示
  - 预计工时：1h

**验收标准**：
- 连续指令可基于上下文正确理解（如先"打开设置"再"点这里"）
- 应用重启后历史记录保留
- 设置面板可配置 AI 相关参数
- 网络异常/设备断开时 UI 显示正确错误信息
- 所有错误场景不导致应用崩溃

---

### Iteration 18：点击录制功能 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 实现"点击录制"功能：手动在投屏区域点击目标位置，记录点击坐标并保存到数据库；提供录制模板列表查看、编辑、删除功能 |
| **预计工时** | 12小时 |
| **优先级** | P1（重要） |
| **状态** | **已完成** (2026-06-26) |

**涉及的需求章节**：3.8 点击录制（需求文档 V1.4）

**涉及的设计章节**：3.5 点击录制功能、4.5 RecordClickFormDialog、4.6 RecordedClickListDialog（界面文档 V2.2）

**前置依赖**：
- Iteration 7（设备连接管理）— 需获取设备分辨率用于坐标显示
- Iteration 9（投屏交互操作）— 需复用坐标映射逻辑

**任务清单**：

#### 1. 数据层 — Entity（1h）
- [x] 创建 `src/main/script/entities/recorded-click-entity.ts`
  - 定义 `RecordedClickEntity`（TypeORM Entity），表名 `recorded_clicks`
  - 字段：`id`(INTEGER PK AUTOINCREMENT)、`name`(TEXT)、`x`(INTEGER)、`y`(INTEGER)、`type`(TEXT DEFAULT 'click')、`created_at`(DATETIME)、`updated_at`(DATETIME)
- [x] 更新 `src/main/script/data-source.ts`
  - 在 `entities` 数组中注册 `RecordedClickEntity`
  - 在 `CREATE_TABLES_SQL` 中添加 `recorded_clicks` 建表 DDL

#### 2. 数据层 — Repository（1h）
- [x] 创建 `src/main/script/repositories/recorded-click-repository.ts`
  - `create(params)`：创建录制记录
  - `findAll(page, pageSize)`：分页查询，按 `created_at` DESC 排序，返回 `{ items, total, page, pageSize, totalPages }`
  - `findById(id)`：按 ID 查询
  - `update(id, updates)`：更新名称等字段
  - `delete(id)`：删除记录
  - `count()`：查询总记录数

#### 3. 业务层 — Service（1h）
- [x] 创建 `src/main/script/services/recorded-click-service.ts`
  - 遵循 `ScriptService` 的模式，自动 `ensureInitialized()`
  - 封装 CRUD + 分页查询方法

#### 4. IPC 通信（1.5h）
- [x] 更新 `src/main/script/channels.ts`：新增 4 个通道常量
- [x] 更新 `src/main/script/ipc/handlers.ts`：注册 4 个 `ipcMain.handle` 处理器
- [x] 更新 `src/preload/index.ts`：通过 `contextBridge` 暴露 `recordedClick` API

#### 5. 前端组件 — RecordClickFormDialog（2h）
- [x] 创建 `src/renderer/src/components/RecordClickFormDialog.tsx`
  - Dialog，标题："录制点击事件"
  - 字段：名称(Input 自动聚焦 必填)、坐标X(disabled)、坐标Y(disabled)、类型(disabled="click")
  - 底部：取消（保持录制模式）/ 保存（校验→写入DB→关闭弹窗→退出录制模式）
  - 保存中禁用按钮 + 旋转动画，保存失败显示错误提示
  - Props: `open`, `onOpenChange`, `coord: { x, y }`, `onSaved`

#### 6. 前端组件 — RecordedClickListDialog（3h）
- [x] 创建 `src/renderer/src/components/RecordedClickListDialog.tsx`
  - Dialog，标题："录制的点击事件"，宽度 ~520px
  - 列表区域（最小高度 300px）：
    - **加载中**：居中旋转动画
    - **加载失败**：错误提示 + 重试按钮
    - **数据为空**："暂无录制的点击事件" + 📭 图标
    - **有数据**：每行显示名称、坐标 `(x, y)`、类型 badge、创建时间；右侧编辑/删除按钮
  - 编辑模式：行内名称变为 Input + 保存/取消按钮
  - 删除：ConfirmDialog 确认后删除
  - 分页：底部显示"共 N 条，第 M/P 页" + 上一页/页码/下一页

#### 7. MirrorPanel 集成（2.5h）
- [x] 修改 `src/renderer/src/components/MirrorPanel.tsx`
  - 顶部工具栏右侧新增竖向排列的按钮组
  - **录制按钮**：`CircleDot` 图标，激活态红色脉冲，切换 `isRecording` 状态
  - **查看按钮**：`List` 图标，点击打开列表弹窗
  - **蒙版**：录制模式下投屏区域覆盖 `z-30 bg-black/40 cursor-crosshair` 蒙版
  - 点击蒙版获取坐标（需复用 ScreenCanvas 的坐标映射逻辑 `toDevice`）
  - 按 Escape 退出录制模式

#### 8. 步骤编辑弹窗集成（0.5h）
- [x] `NewStepDialog` 点击步骤增加"从录制记录中选择"按钮
- [x] 打开 `RecordedClickListDialog` 选中记录后自动填充坐标

**验收标准**：
- 点击录制按钮 → 投屏区域出现半透明蒙版，鼠标变为十字准星
- 蒙版上点击 → 弹出表单弹窗，坐标自动填入
- 填写名称点击保存 → 数据写入 `recorded_clicks` 表 → 弹窗关闭 → 退出录制模式
- 点击查看按钮 → 弹出录制模板列表，分页显示
- 列表中可编辑名称、可删除记录
- 点击 Escape 退出录制模式
- 录制按钮激活时有红色脉冲动画反馈

---

### Iteration 19：步骤类型扩展与体验优化 ✅

| 项目 | 内容 |
| :--- | :--- |
| **目标** | 新增回主屏幕(home)和打开App(openApp)步骤类型，实现步骤列表拖拽排序，更新设计文档 |
| **预计工时** | 6小时 |
| **优先级** | P1（重要） |
| **状态** | **已完成** (2026-06-26) |

**涉及的需求章节**：3.3 脚本引擎（F-07 步骤执行）

**涉及的设计章节**：4.3 步骤类型数据格式（脚本管理详细设计）

**任务清单**：

#### 1. 回主屏幕(home)步骤类型（1.5h）
- [x] 后端：`StepType` 添加 `'home'`，定义 `HomeStepData`（无参数）
- [x] 引擎：`StepExecutor` 添加 `executeHome()` → `adbExec.keyEvent('KEYCODE_HOME')`
- [x] 校验：`ScriptValidator` 添加 `'home'` 到合法类型列表
- [x] 转换：`converter.ts` 添加 `'home'` 的序列化/反序列化
- [x] 前端：`NewStepDialog` 添加"回主屏幕"选择按钮（House图标），`StepPanel` 添加标签与图标

#### 2. 打开App(openApp)步骤类型（2.5h）
- [x] 后端：`StepType` 添加 `'openApp'`，定义 `OpenAppStepData`（含 `appName`）
- [x] 引擎：`StepExecutor` 添加 `executeOpenApp()` → `KEYCODE_HOME` → UI Automator 查找 → 点击坐标
- [x] 服务：复制 `uiautomator-service.ts` 到 `script-engine` 目录，改为独立副本；改用 `adb shell cat` 直接读取 XML 到内存
- [x] 校验/转换：添加 `'openApp'` 支持
- [x] 前端：添加"打开App"选择按钮（AppWindow图标），应用名称输入框 + 提示信息

#### 3. 步骤拖拽排序（1.5h）
- [x] 前端：使用 HTML5 Drag & Drop API，添加 `GripVertical` 拖拽手柄
- [x] 后端修复：`step-repository.ts` `updateOrder()` 改为两阶段更新（先设负值再设目标值），解决 `UNIQUE(script_id, step_index)` 约束冲突
- [x] Store：添加 `setScriptSteps()` 方法，`runScript`/`runStep` 执行前刷新步骤
- [x] 交互：悬停时显示拖拽手柄，拖拽中显示蓝色指示线和半透明效果

#### 4. 文档更新（0.5h）
- [x] 更新 `docs/需求.md`：F-07 更新为7种步骤类型，版本表 V1.5
- [x] 更新 `docs/详细设计/脚本管理详细设计.md`：步骤类型表和实体字段说明
- [x] 更新 `docs/开发计划.md`：添加 Iteration 19

**验收标准**：
- ✅ 添加步骤弹窗可选择"回主屏幕"和"打开App"类型
- ✅ 回主屏幕执行后设备回到桌面
- ✅ 打开App通过 UI Automator 定位应用图标并点击启动
- ✅ 步骤列表可拖拽排序，松开后本地立即更新并持久化到数据库
- ✅ 执行全量/单步脚本时使用拖拽后的最新顺序

---

## 3. 已完成功能总览

| 类别 | 功能 | 状态 |
| :--- | :--- | :--- |
| 脚本管理 | 脚本/文件夹创建、编辑、删除 | ✅ |
| 脚本管理 | 脚本导入/导出（.js文件） | ✅ |
| 脚本管理 | 文件夹树形展开/收起 | ✅ |
| 步骤编辑 | 7种步骤类型：点击/输入/滑动/长按/脚本/回主屏幕/打开App | ✅ |
| 步骤编辑 | 新增、编辑、删除步骤 | ✅ |
| 步骤编辑 | 步骤名称自定义、序号自动重排 | ✅ |
| 步骤编辑 | 步骤列表拖拽排序 | ✅ |
| 步骤编辑 | 从录制记录选择坐标 | ✅ |
| 设备连接 | USB设备自动发现 | ✅ |
| 设备连接 | 设备信息获取（型号/分辨率/传输方式） | ✅ |
| 设备连接 | 连接/断开/连接中状态管理 | ✅ |
| 投屏显示 | scrcpy视频流实时解码渲染 | ✅ |
| 投屏显示 | 画布等比缩放自适应 | ✅ |
| 投屏交互 | 单击/滑动/返回/Home | ✅ |
| 投屏交互 | 实时鼠标坐标显示 | ✅ |
| 投屏交互 | 窗口最小化至投屏尺寸 | ✅ |
| 点击录制 | 点击事件录制（蒙版+表单） | ✅ |
| 点击录制 | 录制模板列表/编辑/删除/分页 | ✅ |
| 脚本执行 | 全量执行/单步执行 | ✅ |
| 脚本执行 | 执行进度推送+步骤高亮 | ✅ |
| 脚本执行 | 超时控制/重试机制/中断执行 | ✅ |
| 脚本执行 | ScriptEngine.runSteps() 动态步骤执行 | ❌ |
| 脚本执行 | home(回主屏幕) + openApp(打开App) 步骤类型 | ✅ |
| 系统配置 | 超时时间/步骤间隔/重试次数配置 | ✅ |
| 深色主题 | 深色主题CSS变量、默认深色模式 | ⏳ |
| 跨平台 | macOS打包（dmg） | ✅ |
| 跨平台 | Windows打包 | ❌ |
| AI智能操控 | DeepSeek API集成（Chat + Vision） | ❌ |
| AI智能操控 | LangGraph工作流编排 | ❌ |
| AI智能操控 | 语义指令→ADB控制命令转换 | ❌ |
| AI智能操控 | VLM屏幕视觉分析（元素识别+坐标定位） | ❌ |
| AI智能操控 | AI指令执行面板（AiAgentPanel） | ❌ |
| AI智能操控 | 多轮对话上下文支持 | ❌ |
| AI智能操控 | 历史记录持久化 | ❌ |

## 4. 未实现/待优化项

| 项目 | 说明 | 优先级 |
| :--- | :--- | :--- |
| AI智能操控 - 基础链路 | DeepSeek集成 + 节点实现 + ScriptEngine.runSteps() | P0 |
| AI智能操控 - 工作流与UI | LangGraph图编排 + AI面板UI | P0 |
| AI智能操控 - 体验增强 | 历史记录持久化 + 多轮上下文 + 配置UI | P1 |
| 中文输入 | adb shell input text 不支持中文 | P2 |
| 中文输入 | adb shell input text 不支持中文 | P2 |
| 自动录制 | 自动录制操作生成脚本 | P3 |
| 通知/Webhook | 飞书/钉钉/微信机器人通知 | P3 |
| 定时任务 | Cron表达式定时执行脚本 | P3 |
| 多设备支持 | 同时连接多台设备 | P4 |
| 滚动条样式 | 自定义滚动条样式 | P4 |
Iteration 3 (脚本列表) Iteration 7 (设备连接)
↓ ↓
├──────────────────┐ Iteration 8 (投屏画面)
↓ ↓ ↓
Iteration 4 (导入导出) Iteration 5 (步骤列表) Iteration 9 (投屏交互)
↓
Iteration 6 (步骤编辑)
↓
Iteration 10 (脚本执行引擎) ──────────────────────┐
↓ ↓
Iteration 11 (步骤高亮) Iteration 15 (AI基础链路)
↓ ↓
Iteration 12 (单步执行) Iteration 16 (AI工作流与UI)
↓ ↓
Iteration 13 (深色主题) Iteration 17 (AI体验增强)
↓
Iteration 14 (集成测试)


---

## 5. 风险与应对

| 风险项 | 概率 | 影响 | 应对措施 |
| :--- | :--- | :--- | :--- |
| ADB兼容性问题 | 中 | 高 | 提前验证多设备/多Android版本，准备降级方案 |
| 投屏延迟过高 | 中 | 中 | 调研多种方案（minicap/scrcpy协议），选用最优 |
| 脚本沙箱安全漏洞 | 低 | 高 | 使用成熟的vm2，严格限制权限 |
| Electron打包跨平台问题 | 中 | 中 | 提前测试Windows/macOS环境，配置CI |
| 开发时间超出预期 | 中 | 中 | 优先保证P0功能，P2功能可延后 |

---

## 6. 开发顺序建议

**建议按以下顺序执行：**

1. **第一阶段（基础底座）**：Iteration 1 → 2
2. **第二阶段（脚本核心）**：Iteration 3 → 5 → 6
3. **第三阶段（设备投屏）**：Iteration 7 → 8 → 9（可与第二阶段并行）
4. **第四阶段（执行引擎）**：Iteration 10 → 11 → 12
5. **第五阶段（完善发布）**：Iteration 4 → 13 → 14

**并行开发建议**：
- 完成Iteration 2后，可分配2人分别进行 Iteration 3（脚本）和 Iteration 7（设备）
- 完成Iteration 5后，可分配2人分别进行 Iteration 6（步骤编辑）和 Iteration 8（投屏）

---

**文档状态**：待确认


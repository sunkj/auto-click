# 手机投屏与控制软件 - 产品需求文档 V2.0

| **文档版本** | **修改日期** | **修改人** | **修改内容** |
| :--- | :--- | :--- | :--- |
| V1.0 | 2026-06-03 | AI Assistant | 初始版本创建 |
| V1.1 | 2026-06-03 | AI Assistant | 扩展至Windows+macOS双平台；移除UI章节待独立输出 |
| V1.2 | 2026-06-16 | AI Assistant | 标注各功能实现状态（✅已实现 / ⏳部分实现 / ❌未实现） |
| V1.3 | 2026-06-23 | AI Assistant | 新增3.7节 AI智能操控功能需求（基于LangGraph.js + DeepSeek多模态视觉） |
| V1.4 | 2026-06-26 | AI Assistant | 新增3.8节 点击录制功能需求（独立存储、手動录制、列表查看） |
| V1.5 | 2026-06-26 | AI Assistant | 新增步骤类型：回主屏幕(home)、打开App(openApp)，支持拖拽排序 |
| V1.6 | 2026-06-27 | AI Assistant | 新增步骤类型：AI步骤(ai) — 用户输入自然语言描述，AI自动解析为子步骤执行 |
| V1.7 | 2026-06-28 | AI Assistant | 新增步骤上下文(Context) — 每个步骤支持前置条件判断和写入Context数据 |
| V1.8 | 2026-06-29 | AI Assistant | 新增步骤类型：屏幕文本检测(checkText) — 运行时截图+视觉模型检测屏幕上是否包含指定文本 |
| V1.9 | 2026-06-29 | AI Assistant | 新增步骤类型：图像识别点击(visionClick) — 截图+VLM定位元素坐标后自动点击 |
| V2.0 | 2026-06-30 | AI Assistant | 新增步骤类型：等待(wait) — 脚本执行中插入指定时长的等待步骤，用于耗时场景的延迟处理 |
| V2.1 | 2026-07-02 | AI Assistant | 新增3.13节 文件传输 — 手机文件浏览、文件上传(电脑→手机)、文件下载(手机→电脑) |

## 1. 项目背景与目标

### 1.1 背景

随着移动端应用自动化需求的增长，测试人员、开发者和普通用户需要在PC端对手机进行远程控制，并通过脚本实现自动化操作（如自动化测试、游戏挂机、日常任务自动化等）。

### 1.2 目标

开发一款跨平台的手机投屏管理软件，支持 **Windows 与 macOS**，实现低延迟投屏、键鼠模拟交互、强大的脚本录制与管理功能，并通过机器人通知将运行结果同步至团队协作软件。

## 2. 产品范围 (V1.0)

- **平台支持**：Windows 10/11 (x64) 与 macOS 11+ (Intel/Apple Silicon)
- **移动端**：Android 10+ (基于ADB协议)
- **连接方式**：USB连接（优先），无线Wi-Fi连接（后续支持）
- **设备数量**：单设备连接

## 3. 功能性需求

### 3.1 设备连接与投屏 ✅

| **ID** | **功能名称** | **详细描述** | **实现状态** |
| :--- | :--- | :--- | :--- |
| **F-01** | 设备发现 | 自动检测通过USB连接的Android设备 | ✅ 已实现（adb devices -l） |
| **F-02** | 画面投屏 | 将手机画面实时投射到PC端窗口中，支持窗口缩放 | ✅ 已实现（scrcpy + TinyH264解码） |
| **F-03** | 设备信息 | 显示设备型号、分辨率、连接方式 | ✅ 已实现 |

### 3.2 鼠标键鼠模拟交互 ✅

| **ID** | **功能名称** | **详细描述** | **实现状态** |
| :--- | :--- | :--- | :--- |
| **F-04** | 鼠标点击 | 单击=手指点击，右键返回 | ✅ 已实现（adb shell input tap） |
| **F-05** | 滑动与拖动 | 支持鼠标按住拖动模拟手指滑动 | ✅ 已实现（adb shell input swipe） |
| **F-06** | 文字输入 | 支持键盘输入文本到手机 | ⏳ 部分实现（不支持中文） |

### 3.3 脚本引擎 ✅

| **ID** | **功能名称** | **详细描述** | **实现状态** |
| :--- | :--- | :--- | :--- |
| **F-07** | 步骤执行 | 通过步骤列表执行点击/输入/滑动/长按/等待/回主屏幕/打开App/AI操作/屏幕文本检测/图像识别点击 | ✅ 已实现（11种步骤类型） |
| **F-08** | 脚本执行 | 支持全量/单步执行，实时高亮当前步骤 | ✅ 已实现（IPC进度推送） |
| **F-09** | 绑定元素 | 图片匹配、颜色匹配等高级API | ❌ 未实现 |

### 3.4 录制与脚本生成

| **ID** | **功能名称** | **详细描述** | **实现状态** |
| :--- | :--- | :--- | :--- |
| **F-10** | 操作录制 | 自动录制操作生成脚本 | ❌ 未实现 |
| **F-11** | 手动微调 | 手动编辑步骤参数，设置延迟间隔 | ✅ 已实现 |

### 3.5 脚本管理与任务调度

| **ID** | **功能名称** | **详细描述** | **实现状态** |
| :--- | :--- | :--- | :--- |
| **F-12** | 脚本分组 | 支持创建文件夹分组 | ✅ 已实现（一级目录） |
| **F-13** | 导入导出 | 支持本地导入/导出`.js`脚本文件 | ✅ 已实现 |
| **F-14** | 定时任务 | 支持按小时/分钟/天三种周期定时执行已创建脚本，应用启动时自动恢复定时任务，提供可视化任务管理界面 | ❌ 未实现 |

### 3.6 消息通知与机器人 ❌

| **ID** | **功能名称** | **详细描述** | **实现状态** |
| :--- | :--- | :--- | :--- |
| **F-15** | Webhook配置 | 支持飞书/钉钉/微信机器人 | ❌ 未实现 |
| **F-16** | 事件通知 | 脚本开始/结束通知 | ❌ 未实现 |
| **F-17** | 异常报警 | 异常时发送报警通知 | ❌ 未实现 |

| **ID** | **功能名称** | **详细描述** |
| :--- | :--- | :--- |
| **F-15** | Webhook配置 | 支持配置飞书/钉钉/微信企业版自定义机器人的Webhook地址 |
| **F-16** | 事件通知 | 脚本开始时发送“开始运行”通知，脚本结束时发送“运行结束/成功”通知 |
| **F-17** | 异常报警 | 脚本运行报错（如找不到图片）或设备断开连接时，发送红色报警通知，附携带错截图 |
### 3.7 AI智能操控 ✅

| **ID** | **功能名称** | **详细描述** | **实现状态** |
| :--- | :--- | :--- | :--- |
| **F-18** | AI语义指令转控制命令 | 用户输入自然语言描述（如"打开微信"、"点击微信"、"滑动到下一屏"等），系统自动解析并转换为多步执行序列 | ✅ 已实现 |
| **F-19** | 多模态视觉感知 | 在执行指令前自动截取当前手机屏幕画面，结合智谱GLM-4V多模态视觉模型对屏幕内容进行识别和理解（如检测图标位置、文本内容），为指令转换提供精准的上下文坐标信息 | ✅ 已实现 |
| **F-20** | LangGraph编排工作流 | 基于LangGraph.js构建有向图工作流编排引擎，将"意图解析→截图→视觉检测→步骤转换"组织为可观测的Agent工作流节点；同时支持纯文本指令直接转换（如回主屏幕、返回等） | ✅ 已实现 |
| **F-24** | AI步骤类型 | 脚本步骤中新增AI类型，用户可在步骤中嵌入自然语言描述，执行时AI自动解析为子步骤序列并执行 | ✅ 已实现 |
| **F-25** | 多任务连续执行 | AI助手支持一次性处理复合指令（如"打开微信，进入某某聊天窗口，输入Hello，发送"），自动分解为多步连续操作 | ✅ 已实现 |
| **F-26** | 屏幕文本检测 | 支持"检查屏幕上是否包含指定文本"的指令，通过视觉模型检测文本是否存在并返回结果 | ✅ 已实现 |
| **F-32** | 等待步骤 | 脚本执行过程中插入延时等待，支持自定义等待时长（秒级），适用于页面加载、动画播放、数据刷新等需要等待的场景 | ❌ 未实现 |

#### 3.7.1 技术方案

| **层级** | **技术选型** | **说明** |
| :--- | :--- | :--- |
| AI编排框架 | LangGraph.js | 使用LangGraph的Node.js版本（@langchain/langgraph）构建有状态的工作流，管理多步骤推理与工具调用 |
| 视觉模型 | 智谱GLM-4V | 通过HTTP API调用智谱多模态大模型，对手机屏幕截图进行视觉理解，识别UI元素位置和文本内容 |
| 大语言模型 | DeepSeek Chat | 在意图理解和指令解析环节使用DeepSeek Chat模型，将用户自然语言解析为结构化动作参数 |
| 录制的点击模板 | RecordedClick（TypeORM + SQLite） | 录制的点击事件作为动态工具注入AI提示词，AI可优先使用已录制的坐标 |
| 命令执行适配层 | ADB Shell | 将解析后的动作（tap、swipe、input text等）通过ADB发送至设备执行 |

#### 3.7.2 工作流设计（LangGraph Agent）

```
用户输入 → [意图理解节点(LLM)]
                    │
          ┌─────────┴─────────┐
          ▼                    ▼
    [坐标类指令]           [纯按键指令/check_text]
          │                    │
          ▼                    │
   [截屏节点(ADB)]            │
          │                    │
          ▼                    │
   [视觉检测节点(VLM)]        │
    (检查屏幕上是否有指定     │
     文本/元素)               │
          │                    │
          └─────────┬─────────┘
                    ▼
           [步骤转换节点]
                    │
                    ▼
      ┌─────────────────────┐
      │  AI执行引擎          │
      │  (遍历步骤逐条执行)   │
      └─────────────────────┘
```

**实际工作流路由逻辑**：

| 条件 | 路径 | 说明 |
| :--- | :--- | :--- |
| 指令包含`check_text` | 意图解析 → 截图 → 视觉检测 → 步骤转换 | 需要视觉模型验证文本是否存在 |
| 指令需要坐标（点击/滑动等） | 意图解析 → 步骤转换（直接使用已录制的点击坐标或LLM返回坐标） | 不需要视觉检测，直接执行 |
| 纯按键指令（回主屏幕/返回） | 意图解析 → 步骤转换 | 跳过截图和视觉分析 |

**各节点职责**：

| **节点名称** | **输入** | **处理逻辑** | **输出** |
| :--- | :--- | :--- | :--- |
| **意图理解** | 用户自然语言指令 | 调用DeepSeek Chat分析指令意图，提取动作类型和参数；配合已录制的点击模板作为动态工具，AI可引用已有坐标 | 结构化意图对象（支持多步骤sequence） |
| **截屏** | 设备serial | 调用ADB截取当前手机屏幕，保存为Base64图像 | 屏幕截图Base64 |
| **视觉检测** | 屏幕截图 + 目标描述 | 调用智谱GLM-4V多模态模型，检测屏幕上是否包含指定文本/元素 | 检测结果（是否匹配+描述） |
| **步骤转换** | 意图对象 + 检测结果 | 将AI解析结果映射为EngineStep[] | 引擎步骤列表 |
| **引擎执行** | 引擎步骤列表 | 遍历步骤，调用StepExecutor逐条执行 | 执行结果 |

#### 3.7.3 典型场景示例

| **用户指令** | **意图理解结果** | **执行步骤序列** |
| :--- | :--- | :--- |
| "点击微信" | `{ action: "tap", target: "微信", ... }` | 使用已录制坐标或LLM返回坐标 → `input tap x y` |
| "滑动到下一屏" | `{ action: "swipe", params:{direction:"left"} }` | 计算中心坐标 → `input swipe x1 y1 x2 y2 300` |
| "回主屏幕" | `{ action: "keyEvent", params:{key:"HOME"} }` | `input keyevent KEYCODE_HOME` |
| "打开微信，进入陈晓蓓聊天，输入Hello，发送" | `{ action: "sequence", steps: [openApp, tap, type, tap] }` | 打开微信 → 点击联系人 → 输入文本 → 点击发送（4步连续执行） |
| "检查屏幕是否包含'发送'按钮" | `{ action: "check_text", target: "发送" }` | 截图 → 调用GLM-4V检测 → 返回是否匹配 |

#### 3.7.4 关键技术难点与对策

1. **动态UI适配**
   - *难点*：不同App、不同版本的UI布局差异巨大，AI可能误判。
   - *对策*：提供录制的点击模板作为动态工具，AI优先使用已录制的坐标；同时支持用户手动录制并命名，提高准确率。

2. **执行时序与动效等待**
   - *难点*：执行步骤后界面动效未完成（如页面切换动画），导致下一步操作位置错误。
   - *对策*：步骤间默认间隔3秒（可配置），AI解析的连续操作间自动插入延迟。

3. **多步骤复合指令**
   - *难点*：用户一条指令可能包含多个操作（"打开微信，发送消息"），需要分解为多步并处理依赖关系。
   - *对策*：意图解析阶段支持`sequence`类型，将复合指令拆分为多步IntentResult，步骤转换时逐条映射为EngineStep[]并顺序执行。

4. **模型调用成本**
   - *难点*：频繁调用大模型API会产生较高费用和延迟。
   - *对策*：录制的点击模板作为工具优先使用，仅在复杂场景（如check_text）调用视觉模型。纯按键指令（home/back）完全不调用模型。

#### 3.7.5 依赖与集成

| **依赖项** | **用途** | **集成方式** |
| :--- | :--- | :--- |
| @langchain/langgraph | AI工作流编排引擎 | npm包，在Electron主进程中运行Agent图 |
| @langchain/core | LangChain基础库（消息模型、工具定义） | npm包 |
| deepseek API (openai-compatible) | 调用DeepSeek Chat模型进行意图理解 | HTTP API，通过LangChain的ChatOpenAI封装调用 |
| 智谱GLM-4V API | 多模态视觉分析（文本检测、元素识别） | HTTP API，直接fetch调用 |
| node-adb / adbkit | ADB协议封装 | npm包，用于截屏和发送命令 |
| sharp / jimp | 图片处理（缩放、裁剪、Base64编解码） | npm包，预处理截图后再发送给视觉模型 |

### 3.8 点击录制 ❌

| **ID** | **功能名称** | **详细描述** | **实现状态** |
| :--- | :--- | :--- | :--- |
| **F-21** | 点击事件录制 | 用户通过可视化方式在投屏区域点击，自动记录点击坐标、类型，存储为可复用的点击模板 | ❌ 未实现 |
| **F-22** | 录制模板管理 | 查看已录制的点击模板列表，支持分页、编辑名称、删除模板 | ❌ 未实现 |
| **F-23** | 录制模板复用 | 录制的点击模板可被AI智能操控（F-18→F-20）调用，也可在添加脚本步骤时直接选择使用 | ❌ 未实现 |

#### 3.8.1 数据模型

点击录制数据存储于独立的 `recorded_clicks` 表，与脚本步骤表（`steps`）解耦，作为跨功能的公用模板库。

| **字段** | **类型** | **说明** |
| :--- | :--- | :--- |
| `id` | INTEGER (PK, AUTOINCREMENT) | 自增主键 |
| `name` | TEXT | 用户自定义名称，如"点击微信"、"点击支付宝" |
| `x` | INTEGER | 点击坐标 X（设备物理像素） |
| `y` | INTEGER | 点击坐标 Y（设备物理像素） |
| `type` | TEXT | 动作类型，固定为 `"click"`（预留后续扩展 swipe、longpress 等） |
| `created_at` | DATETIME | 创建时间 |
| `updated_at` | DATETIME | 更新时间 |

#### 3.8.2 交互流程

**流程一：手动录制点击**

```
用户点击录制按钮 → 投屏区域覆盖半透明蒙版 → 鼠标变为十字准星(crosshair)
  → 用户在蒙版上点击目标位置 → 弹出表单弹窗
  → 表单自动填入：坐标(x,y) = 点击位置、type = "click"
  → 用户填写名称（如"点击微信"）→ 点击保存
  → 数据写入 recorded_clicks 表 → 关闭弹窗 → 退出录制模式
```

**流程二：查看与管理录制模板**

```
用户点击查看按钮 → 弹出录制模板列表弹窗
  → 列表分页展示（每页10条），显示：名称、坐标、类型、创建时间
  → 每条记录右侧有「编辑」「删除」操作按钮
  → 编辑：弹出编辑弹窗，可修改名称
  → 删除：确认后删除该记录
```

#### 3.8.3 界面布局

位于 MirrorPanel（投屏面板）右上角，两个按钮垂直排列（竖向排列）：

```
┌──────────────────────────────┐
│  [连接] [断开] [返回] [首页]  │  🔴  ← 录制按钮
│                              │  📋  ← 查看按钮（列表弹窗）
│                              │
│       (投屏区域)              │
│                              │
└──────────────────────────────┘
```

- **录制按钮**：圆角图标按钮，激活时高亮/闪烁，再次点击或保存后退出录制模式
- **查看按钮**：圆角图标按钮，点击弹出录制模板列表弹窗

#### 3.8.4 录制模式详细行为

| **场景** | **行为** |
| :--- | :--- |
| 点击录制按钮（未激活） | 激活录制模式：投屏区域覆盖半透明蒙版（z-index高于画布但低于工具栏），鼠标变为 `cursor: crosshair`，录制按钮高亮 |
| 点击录制按钮（已激活） | 退出录制模式：移除蒙版，恢复鼠标样式，录制按钮恢复正常 |
| 在蒙版区域点击 | 阻止事件穿透到下方画布，解析点击位置的设备物理坐标，弹出表单弹窗 |
| 点击保存 | 校验名称非空 → 写入数据库 → 关闭弹窗 → 退出录制模式 → 若写入失败显示错误提示 |
| 点击取消 | 关闭弹窗，**保持**录制模式（用户可继续点击其他位置录制） |
| 按 Escape 键 | 退出录制模式（移除蒙版） |

#### 3.8.5 查看列表弹窗详细行为

| **场景** | **行为** |
| :--- | :--- |
| 弹窗打开 | 加载第1页数据（每页10条），显示 loading 状态 |
| 分页切换 | 点击页码或上一页/下一页按钮，加载对应页面数据 |
| 点击编辑 | 弹出行内编辑或小弹窗，可修改 `name` 字段，保存后更新数据库并刷新列表 |
| 点击删除 | 弹出确认对话框，确认后删除该记录并刷新列表 |
| 数据为空 | 显示空状态提示"暂无录制的点击事件" |
| 加载失败 | 显示错误提示和重试按钮 |

#### 3.8.6 模板复用入口

录制的点击模板在以下两个场景中可供选择使用：

1. **AI智能操控（F-18→F-20）**：AI助手在执行"点击微信"等指令时，优先查询 `recorded_clicks` 表中名称匹配的模板，直接使用其坐标执行，无需调用VLM视觉分析，提升响应速度和降低API成本。
2. **添加脚本步骤**：在 `NewStepDialog` 新增步骤时，增加"从录制模板选择"选项，打开录制模板列表弹窗，选中后自动填充步骤参数（type=click, x, y）。

#### 3.8.7 技术方案

| **层级** | **技术选型** | **说明** |
| :--- | :--- | :--- |
| 数据持久化 | SQLite (TypeORM + better-sqlite3) | 与脚本管理共用同一数据库实例、同一 DataSource |
| 实体定义 | TypeORM Entity (`RecordedClickEntity`) | 注册到 `AppDataSource`，DDL 同步到数据源初始化流程 |
| IPC通信 | Electron ipcMain.handle / ipcRenderer.invoke | 遵循现有脚本模块的 IPC 模式 |
| 前端UI | React + Tailwind CSS + shadcn/ui | 复用项目中已有的 Button、Dialog、Input 等组件 |
| 图标 | lucide-react | 录制使用 `CircleDot` 或 `Radio`，查看使用 `List` |

### 3.9 步骤上下文 Context ❌

| **ID** | **功能名称** | **详细描述** | **实现状态** |
| :--- | :--- | :--- | :--- |
| **F-27** | 前置条件判断 | 每个步骤可配置前置条件，从 Context 中读取指定 key 的值与目标值比较（等于），满足条件时跳过当前步骤或停止执行 | ❌ 未实现 |
| **F-28** | Context写入 | 每个步骤执行完成后可将任意 key-value 写入 Context，供后续步骤使用 | ❌ 未实现 |
| **F-29** | 模板语法 | 步骤参数支持 `{{key}}` 模板语法，执行时自动替换为 Context 中的值 | ❌ 未实现 |

#### 3.9.1 数据模型

```typescript
/** 前置条件 */
interface StepCondition {
  key: string                   // Context 中的 key
  value: string                 // 比较值（支持 {{模板}}）
  onMatch: 'skip' | 'stop'     // 满足条件时的行为：跳过当前步骤 / 停止整个脚本
}

/** Context 写入 */
interface StepContextOutput {
  key: string                   // 要写入的 key
  value: string                 // 要写入的值（支持 {{模板}}）
}
```

存储在步骤的 data 字段中：
```json
{
  "x": 320, "y": 580,
  "_condition": { "key": "loggedIn", "value": "false", "onMatch": "skip" },
  "_context": { "key": "lastClick", "value": "微信" }
}
```

#### 3.9.2 执行流程

```
1. 解析步骤参数中的 {{模板}} → 替换为 context 实际值
2. 检查前置条件：
   - 如果 context[key] === value → 按 onMatch 跳过或停止
   - 否则 → 正常执行
3. 执行步骤（click/type/swipe 等）
4. 写入 Context（如果配置了 _context）
```

#### 3.9.3 使用场景

| 场景 | 配置 |
| :--- | :--- |
| 条件执行 | AI 先判断屏幕状态，将结果写入 context，后续步骤根据结果决定是否跳过 |
| 动态参数 | 点击坐标由前一步 AI 识别后写入 context，后续 click 步骤用 `{{x}}` `{{y}}` 引用 |
| 数据传递 | AI 步骤提取的联系人名称、输入文本等数据传递给后续步骤 |

---

### 3.10 屏幕文本检测步骤 ✅

（已实现，见前文 3.10.1~3.10.6）

### 3.11 等待步骤 ❌

| **ID** | **功能名称** | **详细描述** | **实现状态** |
| :--- | :--- | :--- | :--- |
| **F-32** | 等待步骤 | 新增 `wait` 步骤类型，用户可配置等待时长（秒），执行时脚本暂停指定时长后继续，适用于页面加载、动画播放、数据刷新等需要等待的场景 | ❌ 未实现 |

#### 3.11.1 背景与动机

在自动化脚本执行过程中，许多操作后需要等待一段时间才能继续下一步。例如：

- **页面跳转/加载**：点击某个按钮后，新页面需要时间加载完成
- **动画播放**：App 内的过渡动画尚未结束，直接操作可能导致坐标偏移
- **数据刷新**：下拉刷新后需要等待数据加载完成
- **网络请求等待**：提交表单后等待服务端响应
- **游戏场景**：游戏中的冷却时间、动画播放等待

现有的步骤间默认间隔（stepInterval）虽然提供了基础的延迟，但在某些场景下需要更精确、更长的等待时间，且不同步骤后的等待时长往往不同。如果仅靠全局 stepInterval，要么延迟不足导致失败，要么过度延迟浪费时间。

因此需要一种专门的「等待」步骤类型，让用户可以在步骤列表中灵活插入任意时长的等待。

#### 3.11.2 数据模型

```typescript
/** 等待步骤参数 */
interface WaitStepData {
  duration: number          // 等待时长，单位：秒（支持小数，如 0.5 表示 500ms）
  description?: string
}
```

#### 3.11.3 执行流程

```
1. 解析 duration 参数，转为毫秒（duration × 1000）
2. 打印日志：[StepExecutor] ⏳ 等待 N 秒...
3. 通过 setTimeout / Promise 延时等待指定时长
4. 等待结束后，打印日志：[StepExecutor] ✔ 等待完成
5. 继续执行下一步骤
```

**关键设计**：
- 等待不依赖任何 ADB 命令或设备状态，纯本地执行
- 等待期间可通过 `execution-queue.ts` 的 cancel 机制中断（如用户手动停止脚本）
- duration 支持小数（如 0.5 表示 500ms），精度到毫秒
- description 字段用于说明等待原因（如"等待页面加载"），便于脚本可读性

#### 3.11.4 使用场景示例

| 场景 | 步骤序列 | 说明 |
| :--- | :--- | :--- |
| 打开App后等待 | ① openApp "微信" → ② wait 3s → ③ click 联系人 | 等待微信首页加载完成 |
| 页面跳转等待 | ① click 登录按钮 → ② wait 2s → ③ checkText "登录成功" | 等待页面跳转后再检查 |
| 游戏冷却等待 | ① click 攻击 → ② wait 30s → ③ click 技能 | 等待技能冷却 |
| 数据加载等待 | ① swipe down → ② wait 1.5s → ③ click "查看更多" | 等待下拉刷新完成 |
| 多步持续等待 | ① wait 60s → ② checkText "更新完成" | 等待自动更新完成 |

#### 3.11.5 与 stepInterval 的关系

- **stepInterval**（全局配置）：每个步骤执行完成后到下一步开始前的基础等待时间，默认 3 秒
- **wait 步骤**（局部指令）：在特定位置插入更长的等待时间
- 两者互不冲突，wait 步骤自身也会受到 stepInterval 的影响（wait 步骤执行完成后，再等待 stepInterval 才进入下一步）

### 3.12 图像识别点击步骤 ❌

| **ID** | **功能名称** | **详细描述** | **实现状态** |
| :--- | :--- | :--- | :--- |
| **F-31** | 图像识别点击步骤 | 新增 `visionClick` 步骤类型，用户配置「目标描述」参数，执行时自动截取当前手机屏幕，通过多模态视觉模型（VLM）识别目标元素在屏幕上的位置坐标，然后执行点击操作 | ❌ 未实现 |

#### 3.12.1 背景与动机

现有的 `checkText` 步骤只能检测文本是否存在但不能交互。AI 智能操控的 `tap` 动作在无录制坐标时已通过 VLM 兜底实现了识别点击，现在需要将这个能力封装为独立的脚本步骤，让用户在步骤列表中直接使用。

#### 3.11.2 数据模型

```typescript
/** 图像识别点击步骤参数 */
interface VisionClickStepData {
  target: string            // 目标文本/元素描述，如"钱包"、"发送按钮"
  description?: string
}
```

#### 3.11.3 执行流程

```
执行 visionClick("钱包")
  → 截图（adb exec-out screencap -p）
  → 调用 VLM：找到"钱包"位置，返回归一化坐标 (0~1)
  → 坐标还原：设备X = VLM_X × 原始宽度
  → 坐标还原：设备Y = VLM_Y × 原始高度
  → adb shell input tap 设备X 设备Y
  → 完成
```

#### 3.11.4 使用场景

| 场景 | 说明 |
| :--- | :--- |
| 点击文案按钮 | 知道按钮叫什么但不知道精确坐标 |
| 动态UI适配 | 不同版本 UI 布局变化，文字位置可能不同 |
| 与 checkText 配合 | 先识别点击，再检测是否到达目标页面（写 Context） |

#### 3.11.5 技术方案

| 层级 | 技术选型 | 说明 |
| :--- | :--- | :--- |
| 视觉定位引擎 | 智谱 GLM-4V（多模态视觉模型） | 复用 ai-agent 模块现有的 VLM 服务 |
| 截图方式 | `adb exec-out screencap -p` | 复用现有截图服务 |
| 坐标系统 | 归一化坐标 (0~1) × 设备分辨率 | 避免 VLM 内部图片处理导致的坐标系偏差 |
| 步骤执行 | StepExecutor.execute() | 新增 `case 'visionClick'` 分支处理 |

| **ID** | **功能名称** | **详细描述** | **实现状态** |
| :--- | :--- | :--- | :--- |
| **F-30** | 屏幕文本检测步骤 | 新增 `checkText` 步骤类型，用户可配置「目标文本」参数，执行时自动截取当前手机屏幕，通过视觉模型（智谱GLM-4V）检测屏幕上是否包含指定文本，检测结果（matched/not matched）可写入 Context 供后续步骤的前置条件判断使用 | ❌ 未实现 |

#### 3.10.1 背景与动机

现有的 AI 智能操控模块（3.7节）已支持通过自然语言指令 `check_text` 检测屏幕文本，但该能力仅限 AI Agent 内部使用，无法以独立步骤形式嵌入脚本流程中。用户需要一种**可编排的、可视化的**屏幕文本检测能力：

- **条件分支**：先检测屏幕上是否出现"成功"文字，再决定继续执行或停止
- **循环等待**：循环检测某个文本出现后再执行下一步操作
- **状态判断**：检测当前是否在特定页面或弹窗中，将结果写入 Context 驱动后续流程

#### 3.10.2 数据模型

```typescript
/** 屏幕文本检测步骤参数 */
interface CheckTextStepData {
  text: string                       // 目标文本，如"成功"、"发送"、"确认"
  description?: string               // 步骤描述，如"检测是否出现'成功'文字"
  matchedContext?: {
    key: string                      // 匹配成功时写入的 Context Key
    value: string                    // 匹配成功时写入的 Context Value
  }
}
```

#### 3.10.3 执行流程

```
执行到 checkText 步骤
    │
    ├── 1. 从步骤参数中提取目标文本（如"成功"）
    │
    ├── 2. 调用 ADB 截取当前手机屏幕（adb exec-out screencap -p）
    │      └── 将截图缩放编码为 Base64
    │
    ├── 3. 调用智谱 GLM-4V 视觉模型检测文本
    │      └── 返回 { matched: boolean, description: string }
    │
    ├── 4. 如果 matched === true 且配置了 matchedContext
    │      └── context[matchedContext.key] = matchedContext.value
    │
    └── 5. 步骤执行完毕，返回结果
```

#### 3.10.4 使用场景示例

| **场景** | **步骤配置** | **执行行为** |
| :--- | :--- | :--- |
| 等待加载完成 | 步骤1: checkText("加载中") + 前置条件 stop; 步骤2: checkText("完成") → onMatch 写入 ctx.done="yes" | 先检测"加载中"，存在则停止；再检测"完成"，匹配后写 ctx |
| 登录成功校验 | checkText("登录成功") → onMatch 写入 ctx.loginResult="ok" | 检测到"登录成功"时写入 ctx.loginResult="ok" |
| 异常弹窗检测 | checkText("网络连接失败") → onMatch 写入 ctx.hasError="true" | 检测到错误弹窗时写入 ctx.hasError，后续步骤据此停止 |
| 页面跳转确认 | checkText("我的") → onMatch 写入 ctx.onProfile="1" | 检测到"我的"文字说明已进入个人页，后续步骤继续执行 |

#### 3.10.5 与 Context 的集成

checkText 步骤不自动写入固定 Context key，而是由用户在配置步骤时**手动指定**匹配成功时要写入的 Key 和 Value。

用户可在 checkText 步骤参数区的"✅ 匹配成功时写入上下文"中设置：

| 字段 | 说明 |
| :--- | :--- |
| 选择 Key | 从脚本初始上下文的 Key 列表中选择 |
| 设置 Value | 匹配成功时要写入的值 |

配合前置条件（F-27）可实现条件分支逻辑：

```
步骤1: checkText("登录成功")
       └─ 匹配成功时 → context["loginResult"] = "ok"
步骤2: click(100, 200)
       └─ 前置条件: loginResult = "ok" → 跳过（登录成功，跳到步骤3）
       或 前置条件: loginResult ≠ "ok" → 停止（登录失败，停止脚本）
```

#### 3.10.6 技术方案

| **层级** | **技术选型** | **说明** |
| :--- | :--- | :--- |
| 文本检测引擎 | 智谱 GLM-4V（多模态视觉模型） | 复用 ai-agent 模块中现有的 `checkScreenText` 工具函数 |
| 截图方式 | `adb exec-out screencap -p` | 复用 ai-agent 模块中现有的截图服务 |
| 图片处理 | sharp | 截图缩放至最大 1024px，减少 API 调用成本 |
| 步骤执行 | StepExecutor.execute() | 新增 `case 'checkText'` 分支处理 |
| Context 集成 | matchedContext 条件写入 | 仅匹配成功时写入用户配置的 Key-Value |

### 3.13 文件传输

| **ID** | **功能名称** | **详细描述** | **实现状态** |
| :--- | :--- | :--- | :--- |
| **F-33** | 手机文件浏览 | 在侧栏新增「文件」Tab，通过 ADB 列出手机端文件目录，支持逐级进入子目录、返回上级目录、面包屑导航快速跳转 | ✅ 已实现 |
| **F-34** | 文件上传（电脑→手机） | 用户通过文件选择对话框选取电脑本地文件，上传到手机当前浏览目录中；上传后自动触发 MediaStore 扫描，图库中立即可见 | ✅ 已实现 |
| **F-35** | 文件下载（手机→电脑） | 选中手机端文件后，通过 ADB pull 下载到电脑系统下载目录（`~/Downloads`）；重名自动加时间戳后缀 | ✅ 已实现 |
| **F-36** | 文件列表可视化 | 按目录/文件分类展示，目录在前、文件在后，各自按修改时间倒序排列；文件根据扩展名显示不同图标；图片文件懒加载缩略图；两栏布局（文件名+时间/大小） | ✅ 已实现 |

#### 3.13.1 背景与动机

在自动化脚本运行过程中，用户经常需要将 APK 安装包、测试素材、截图等文件传输到手机，或将手机中的日志、截图、录屏等文件传输到电脑。目前用户需要额外使用 Android File Transfer、adb 命令行或其他第三方工具来完成文件传输，操作繁琐且打断工作流。

将文件传输功能集成到 AutoClick 中，可以：
- **减少工具切换**：用户无需切换到其他工具即可完成文件传输
- **统一工作流**：在投屏、脚本管理、AI 操控的同一界面中完成文件操作
- **降低使用门槛**：图形化文件浏览和操作，无需记忆 adb 命令

#### 3.13.2 交互流程

**流程一：浏览手机文件**

```
用户点击「文件」Tab → 自动加载 /sdcard 目录内容
  → 显示文件/目录列表（目录在前，文件在后）
  → 点击目录 → 进入该目录 → 刷新列表
  → 点击面包屑路径段 → 快速跳转到对应层级
  → 点击「..」→ 返回上级目录
```

**流程二：上传文件**

```
用户点击「📤 上传文件」按钮
  → 弹出系统文件选择对话框（支持所有文件类型）
  → 选中文件后确认
  → 文件通过 ADB push 上传到手机当前目录
  → 上传完成后自动刷新列表
  → 显示成功/失败通知
```

**流程三：下载文件**

```
用户在列表中选择一个文件（单击选中高亮）
  → 点击「📥 下载选中」按钮
  → 文件通过 ADB pull 下载到电脑 ~/Downloads 目录
  → 如果已存在同名文件，自动添加时间戳后缀
  → 显示成功/失败通知（成功时显示保存路径）
```

#### 3.13.3 界面布局

在侧栏的 Tab Bar 中新增「文件」Tab（位于"AI 助手"之后），Tab 内容如下：

```
┌──────────────────────────────────┐
│  /sdcard  >  Download  >  files  │  ← 面包屑导航
├──────────────────────────────────┤
│  [📁  ..]                        │  ← 返回上级（根目录不显示）
│  [📁  Android]                   │  ← 目录项
│  [📁  DCIM]                      │
│  [📁  Download]                  │
│  [📄  test.txt]      1.2 KB     │  ← 文件项
│  [📄  photo.jpg]     2.3 MB     │
│  [📄  video.mp4]     15.6 MB    │
│  ...                            │
├──────────────────────────────────┤
│  [📤 上传文件]  [📥 下载选中]    │  ← 操作按钮
│  当前: /sdcard  ·  12 项         │  ← 底部状态
└──────────────────────────────────┘
```

#### 3.13.4 技术方案

| **层级** | **技术选型** | **说明** |
| :--- | :--- | :--- |
| 文件传输协议 | ADB push/pull 命令行 | 复用项目中已有的 ADB 集成，无新增依赖 |
| 文件浏览 | `adb shell ls -l` + `adb shell stat` | 标准 Linux 命令，兼容所有 Android 设备 |
| 文件对话框 | Electron `dialog.showOpenDialog` | 原生系统文件选择器 |
| 下载目录 | Electron `app.getPath('downloads')` | 获取系统标准下载目录 |
| 前端UI | React + Tailwind CSS + shadcn/ui | 复用项目中已有的组件体系 |
| 状态管理 | Zustand | 新增 fileTransferStore |
| IPC通信 | Electron ipcMain.handle / ipcRenderer.invoke | 遵循现有模块的 IPC 模式 |
| 图标 | lucide-react | Folder/File/Image/Video/Music/Package/Archive |

#### 3.13.5 关键技术难点与对策

1. **ADB 命令解析**
   - *难点*：`adb shell ls -l` 输出格式在不同 Android 版本和设备上可能有差异。
   - *对策*：使用稳定的 `-l` 格式，按空格分割解析；对特殊文件名（含空格）做转义处理。

2. **大文件传输**
   - *难点*：大文件（如视频、APK）通过 ADB push/pull 传输时间较长，界面可能卡顿。
   - *对策*：在异步 IPC 中执行命令，界面显示 loading 动画，不阻塞用户操作。

3. **设备断开**
   - *难点*：传输过程中设备断开会导致命令失败。
   - *对策*：操作前检查设备连接状态；失败时提供明确的错误提示。

4. **中文文件名**
   - *难点*：中文文件名在 adb shell 输出中可能乱码。
   - *对策*：使用 ADB 的编码处理，确保文件名正确显示和传输。

---

## 4. 非功能性需求

| **ID** | **类别** | **要求描述** |
| :--- | :--- | :--- |
| **N-01** | 性能 | CPU占用率空闲<5%，投屏时<15%；显存占用<500MB |
| **N-02** | 兼容性 | 支持1920x1080及以上主流分辨率，兼容华为、小米、三星、OPPO、vivo等主流品牌 |
| **N-03** | 跨平台一致性 | Windows与macOS版本核心功能完全一致，配置文件和脚本格式互通 |
| **N-04** | 安全性 | 脚本存储需加密（防止源码泄露）；ADB连接仅限本地，不得私自上传用户手机文件 |

## 5. 附录：技术实现建议

### 5.1 核心技术选型建议

- **投屏核心**：使用 **scrcpy** (v4.0+) 作为底层服务。其本身支持Windows和macOS双平台，可大幅降低跨平台开发成本。
- **交互控制**：利用 scrcpy 的 `--hid-mouse` 和 `--hid-keyboard` 参数实现HID模拟。此模式将电脑鼠标模拟为“物理外挂”，兼容性最佳。
- **脚本引擎**：使用 **微软的 ClearScript** (V8引擎) 或 **Node.js** 嵌入式版本来运行JavaScript。
- **跨平台UI框架**：
  - **Qt (C++)**：scrcpy原生基于Qt，集成度高，资源占用低，推荐使用。
  - **Electron (Web技术)**：开发效率高但内存占用较高，macOS上体验尚可。
  - **Flutter**：桌面端支持正在成熟，也可考虑。

### 5.2 跨平台注意事项

- **ADB工具路径**：Windows使用`adb.exe`，macOS使用`adb`（需处理权限`chmod +x`）。软件需自动识别操作系统并调用对应可执行文件。
- **USB权限**：macOS无需驱动但可能需要用户授权“屏幕录制”权限才能捕获输入；Windows需要正确的ADB驱动。
- **窗口管理**：双平台窗口API不同，使用Qt可统一抽象层。

### 5.3 难点攻克与参考

1. **坐标适配问题**
   - 手机分辨率与PC窗口分辨率不同。脚本录制的坐标必须是 **相对坐标**（例如 `0.5, 0.5` 代表屏幕中心），执行时再根据当前设备分辨率换算为绝对坐标，否则换手机后脚本会失效。

2. **即时通讯机器人格式差异**
   - 虽然都叫Webhook，但飞书、钉钉、微信的JSON结构完全不同。
   - *建议*：开发一个 **“通知适配层”** 。用户选择“飞书”后，软件自动套用对应的MsgType模板，仅需用户填写密钥。

3. **连接稳定性**
   - ADB连接容易因手机锁屏、USB线松动断开。
   - *建议*：实现 **守护进程**。通过读取 `adb devices` 状态，发现设备离线自动重连（`adb reconnect`），确保无人值守的自动化任务能够7x24小时运行。
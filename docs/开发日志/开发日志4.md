


# AutoClick - 用户问题记录（脚本管理 Service 部分）

为什么又回到详细设计部分呢？

因为在开始开发计划 Iteration 3 的时候，发现数据全是mock的，并没有生成实际的数据管理。实际上也符合我的预期，先把静态UI界面画好，这样也是详细需求整理确认阶段，所有的需求和想法先落实下来，通过UI来看是最直观的。

---

## 问题 1

现在，我们来整理一下服务层的设计需求，先看脚本管理部分，服务层设计一个脚本管理service，需求包括：

1. 管理脚本的数据，包括新增，编辑，删除，更新。
2. 管理脚本的step数据，包括新增，编辑，删除，更新。
3. 设计一下表结构。

---

## 问题 2

修改一下：

1. 脚本表增加一个排序字段

---

## 问题 3

scriptservice部分加上技术实现设计，包括准备用什么orm框架

---

## 问题 4

好，那就选用TypeORM，展示不需要列出代码实现部分，保留目录结构即可

---

## 问题 5

生成markdown文档

---

## 问题 6

脚本设计部分的对话，帮忙生成markdown文档

---

生成 [脚本管理详细设计.md]，下面就可以再次回到 Iteration 3 实现 Service 层的代码。


# 脚本管理 Service 实现

## 1. 实现脚本管理 Service 层

> 上一个阶段 Iteration 3 只是 mock 数据，并没有真正实现后端代码，下面：
> 1. 请根据这份设计文档实现脚本管理部分的 service 层代码
> 2. 暂时不要对接到 renderer 层，我要先 review 一下

---

## 2. 对接到 Renderer 层

> 好，开始对接到 renderer 层

---

## 3. 新建脚本没有生效

> 新建脚本没有生效，控制台有错误
> 
> ```
> [4107:0614/101914.910221:ERROR:CONSOLE(1)] "Request Autofill.enable failed...
> ```

---

## 4. 还是不行（数据库文件未创建）

> 还是不行

---

## 5. 还是不行，数据库文件也没有看到创建出来

> 还是不行，数据库文件也没有看到创建出来

---

## 6. 控制台有错误（Preload 加载失败）

> 控制台有错误
> 
> ```
> VM4 sandbox_bundle:2 Unable to load preload script: ...
> Error: module not found: ../shared/ipc-channels
> ```

---

## 7. 脚本创建成功，但控制台有 SQLITE_ERROR 事务错误

> 脚本创建成功了，但是 cli 控制台启动的时候有这样的错误
> 
> ```
> query failed: BEGIN TRANSACTION
> error: Error: SQLITE_ERROR: cannot start a transaction within a transaction
> ```

---

## 8. 还有这样的错误（migrations 表已存在）

> 还有这样的错误
> 
> ```
> query failed: CREATE TABLE "migrations" ...
> error: Error: SQLITE_ERROR: table "migrations" already exists
> ```

---

## 9. 这个是什么错误（Autofill 错误）

> 这个是什么错误
> 
> ```
> [ERROR:CONSOLE(1)] "Request Autofill.enable failed...
> ```

---

## 10. 添加 Step 出错（uesScriptStore 未定义）

> 添加 step 出错
> 
> ```
> NewStepDialog.tsx:44 Uncaught (in promise) ReferenceError: useScriptStore is not defined
> ```

---

## 11. 三个功能改进需求

> 有几个问题需要改进：
> 1. 点击第二栏的顶部，脚本名称列最右边的 edit 编辑按钮，需要可以弹出脚本 modal，变成编辑模式，保存后可以更新
> 2. step 管理里面的删除按钮，点击需要弹出 confirm 确认框，同意之后才能删除
> 3. step 管理里面的编辑按钮，点击需要可以弹出新增 step 的 modal 窗，改成编辑，保存之后可以更新到 step 数据

---

## 12. 继续修改 - 新建脚本/目录

> 继续修改：
> 1. 第一栏，新建脚本改一下文案，就新建脚本/目录
> 2. 现有新建脚本的 modal 窗，增加一个新建目录的 tab，可以切换过去新建目录
> 3. 新建目录只支持一级就好，不需要支持多层级
> 4. 包括实现好新建目录的 service 层和 renderer 层，全部对接好

---

## 13. Review 代码

> review 一下代码，有问题的就重构一下

---

## 14. 编辑步骤，删除描述字段

> 编辑步骤，点击类型下面的描述删掉


## 15. 重构代码

> 1. 重构一下主线程代码，把script管理部分的代码放到一个单独的目录
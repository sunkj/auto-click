# 开发日志 7 - Settins实现

---

## 1. 开始 实行settings功能

下一个，第一栏的settings功能实现
1）线删除设备类型的配置
2）实现脚本配置，存储到一个json配置文件就好

## 2. 根据settings来修改script engine

修改script engine，根据settings里面的配置，步骤执行间隔来控制每一步的间隔时间，另外还有脚本总超时时间和失败重试次数

## 3. UI 小调整

> 第二栏顶部的 录制按钮改成停止按钮，点击停止执行脚本
> 执行到哪一步，就高亮显示
> 第二栏，底部右边的执行状态，前面的点，颜色跟着字体一样

> 1）添加step，增加支持输入名称，名称默认为step类型，用户可以修改
> 2）step列表显示名称，超出省略...


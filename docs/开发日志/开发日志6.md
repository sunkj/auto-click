# 开发日志 6 - 脚本执行引擎实现

> 本文档记录了 AutoClick 脚本执行引擎（Iteration 10/11/12）实现过程中的用户反馈与问题。

---

## 1. 开始 Iteration 10/11/12

开始阶段 Iteration 10，11，12，请参考设计文档（docs/详细设计/脚本执行引擎详细设计.md），这里有两个模块需要依赖外部
1）设备管理模块，目前screen-mirror里面有adb的管理，看看是否需抽出来作为以一个共用的模块
2）存储模块，可以调用script下面的脚本管理接口获取到script信息

## 2. 还原 device-manager

src/main/device/device-manager.ts 不要抽出来，还原回src/main/screen-mirror/control.ts，script-engine可以直接引用这里

## 3. 模块耦合问题

screen-mirror 是一个独立的子线程，script-engine又是另外一个独立的子进程，他们直接引用好吗？

## 4. 进程通信方案

我的想法是，是否可以在screen-mirror的子进程通过进程通信的方式或者事件的方式，暴露adb执行命令的方法出去。script-engine通过进程通信的方式把adb执行命令push过去，解耦。script engine只负责脚本的调度和控制逻辑，具体的执行命令由screen mirror提供

## 5. 独立 ADB 连接

如果不改动screen mirror，script engine自己实现一套命令执行，这样有两个adb连接都设备，会不会有问题

## 6. 确认方案

好，就这么做

## 7. 执行没效果

点击执行没有效果，请检查一下，可以先考虑把第一个click事件先调通，点击script下面的单个click step

## 8. 点击坐标不对

点击坐标不对

## 9. 滑动没效果

完成下一个滑动事件

## 10. 滑动不正常

还是不对，好像是点击

## 11. 滑动卡顿

可以点击了，有点卡顿

## 12. 滑动速度调整

有点太快了，降低30%试试

## 13. 长按没生效

长按没有生效，请修复

## 14. 全部失效

现在全部不行了，点击和滑动，长按都不行

## 15. 编译问题排查

（检查编译输出，发现 --noEmit 只检查类型未实际编译）

还是不行

## 16. 连接正常但执行失败

连接状态是connected的，但是step的运行都不行

## 17. 长按步骤保存问题

长按类型step，编辑新增没有保存到数据

## 18. 长按步骤执行问题

[ScriptStore] 执行失败: 步骤 2 类型 "longpress" 不支持

## 19. 执行器问题

可以点击了，滑动都OK了，但是长按没有生效

## 20. 中文输入问题

执行失败 - input text 中文不支持

## 21. 中文输入尝试

（多次尝试：引号包裹、printf编码、spawn+stdin、adb push文件、service call clipboard、cmd clipboard等）

还是失败 - /sdcard/ 不可写

还是失败 - /data/local/tmp/ 不可写

没有发生错误，但是实际上没有写进去

还是失败 - input text 中文报 NullPointerException

## 22. 放弃中文输入

暂时不考虑输入中文，请该用最通用的方式实现

## 23. 配置新步骤类型

添加脚本步骤的modal窗，先删除脚本类型，暂时不需要实现

添加一个长按类型，并实现功能

## 24. UI优化

第一栏底部版本号，改成从package.json里面读取

第二栏，顶部显示脚本名称那里，左箭头不要了，换成脚本列表的文件图标

把UI中的步骤类型翻译成中文（click→点击，swipe→滑动，longpress→长按，home→首页）

## 25. 状态栏

第二栏脚本step的底部右边的状态栏，请显示当前执行的状态，运行中/停止

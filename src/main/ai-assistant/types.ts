/**
 * ai-agent 类型定义
 *
 * 通用类型已迁移至 src/main/common/types.ts。
 * 此处保留 AgentState（含 DynamicToolDef 引用）和 DynamicToolDef 转发。
 */
export * from '../common/types'
export type { AgentState } from '../common/types'
